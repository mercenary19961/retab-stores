# Retab Stores — Project Context

> Quick reference for AI assistants and developers

> **📍 Doc sync:** CLAUDE.md last synced to commit `ced0f79` — 2026-07-29 15:54 (Wed) [`deployment_phase`].
> _Convention: whenever you edit this file, refresh this line to the current commit — run_ `git log -1 --format="%h %cd" --date=format:"%Y-%m-%d %H:%M (%a)"` _and paste the hash + date + time. Anchors the doc to a known code state; pairs with the prose `> Last updated:` log at the bottom of Build Progress._

> **📌 Log the tricky stuff.** Whenever you hit an **issue, blocker, non-obvious behavior, or anything that cost real debugging time**, write it down with its **symptom → root cause → fix** — inline near the relevant section (retab's style, e.g. the MariaDB `db:show` and dual-push `--add --push` notes) and/or a one-liner in the `> Last updated:` log. The same stack is reused across projects (Sky Amman, HardRock, hardrock-ecom-demo), so a gotcha captured once saves the next project too. Traps → document as a gotcha; reusable patterns → note under Architecture/Decisions. When in doubt, over-document.

---

## ⚡ Quick Command Reference (cheatsheet)

> The commands you reach for most, in one place. Fuller context lives in **Local Development** and the relevant Architecture sections. (Dev shell is PowerShell + Git Bash on Windows; these run in either.)

**Run locally**
```bash
composer run dev          # all-in-one: artisan serve + queue + vite  → http://localhost:8000
php artisan serve         # or run the halves separately
npm run dev
```

**Database** (dev = XAMPP **MariaDB** on `127.0.0.1:3307`, db `retab-stores`)
```bash
php artisan migrate
php artisan migrate:fresh --seed     # reset + reseed everything
php artisan migrate:status           # inspect (⚠️ db:show ERRORS on MariaDB — cosmetic; use this)
php artisan db:table <name>
```

**Quality gates** (run before shipping — these are exactly what CI checks)
```bash
php artisan test                     # PHP suite (in-memory sqlite)
php artisan test --filter=<Name>     # a single test or class
npx tsc --noEmit                     # the REAL type gate (Vite/esbuild strips types without checking)
npm run lint                         # eslint . --fix  (lints the WHOLE tree, not per-file)
npm run test:js                      # vitest (front-end helpers, e.g. consent logic)
./vendor/bin/pint                    # PHP formatter (CI runs it in fix mode)
npm run build                        # client + SSR bundle (build:ssr is now an alias — see 2026-07-27)
```

**End-to-end** (Playwright — see Infrastructure → "E2E suite")
```bash
npm run build && npm run test:e2e              # boots a freshly-seeded throwaway sqlite on :8100
npm run test:e2e:report                        # open the last HTML report
QA_BASE_URL=https://<site> npm run test:e2e    # run the specs against a deployment instead
```

**Catalogue + media**
```bash
php artisan catalog:import-zid                 # import the Zid CSV (--fresh wipes first · --no-images to skip)
php artisan catalog:import-images <group> --dir=<folder> --dry-run   # <group> = key in database/data/image-maps.php
php artisan catalog:clean-slugs                # dry-run: junk slugs → clean Arabic + build the 301 map
php artisan catalog:clean-slugs --apply        # ⚠️ run on PROD at launch (this is what creates the redirects)
php artisan media:variants                     # backfill responsive WebP variants (--force to regenerate)
```

**Integrations readiness** (payments + shipping + email — see the command's own docblock)
```bash
php artisan integrations:check                 # per provider: env vars, webhook URL to register, live auth probe
php artisan integrations:check --offline       # env presence only (no network calls)
php artisan mail:test <address>                # send one REAL email through Resend (delivered@resend.dev = safe simulator)
```

**Git** (full workflow under Git & Deploy)
```bash
git ship <branch>        # local merge → main → dual-push both repos → re-sync branch. NEVER the GitHub merge button.
git remote -v            # must show 1 fetch + 2 push URLs on origin (dual-push)
```

**Deploy / prod** (Railway)
```bash
php artisan migrate --force                             # also runs the baseline-data migration
php artisan db:seed --class=AdminUserSeeder --force     # once, AFTER setting ADMIN_EMAIL/ADMIN_PASSWORD env
php artisan optimize                                    # config/route/view/event cache (optimize:clear to undo)
php artisan queue:work                                  # 🔴 REQUIRED as its own service — see Notifications & alerts
```
Set **`APP_URL`** to the real domain — webhook, payment-return, and email URLs are all built from it.

**Dev logins** (seeded in local only): admin `admin@retab.com.sa` / `password` · editor `editor@retab.com.sa` / `password`

---

## Project Overview

**Store:** Retab Stores
**Industry:** E-commerce — premium dates (تمور) retail in Saudi Arabia
**Type:** Online store (storefront + admin/back-office), currently **migrating off [Zid](https://zid.sa)** to a custom Laravel build
**Stack:** Laravel 12 + Inertia.js v3 + React 19 + TypeScript + TailwindCSS v4 (official `laravel/react-starter-kit` v1.0.1; Inertia upgraded v2→v3 on 2026-06-28)
**DB:** Local dev runs on **XAMPP MariaDB 10.4.32** (`127.0.0.1:3307`, db `retab-stores`) — note: MariaDB, _not_ MySQL (XAMPP bundles MariaDB under the "mysql" folder/service name, hence the confusion). **MySQL 8 planned for production** (switch via `DB_CONNECTION`); dev↔prod engine parity is a pending decision (see Foundation).
**Mailer:** _TBD_ — likely Resend (mirroring Sky Amman), decision pending
**Architecture:** Single-service monolith (Laravel serves everything via Inertia). SSR build-time scaffolding present; runtime SSR not yet wired (see Build Progress).
**Hosting:** _TBD_ — likely Railway (FrankenPHP) behind Cloudflare, mirroring Sky Amman
**Branch:** `main`
**Repos (dual-push):** `origin` fetches from `https://github.com/mercenary19961/retab-stores.git`; a **second push URL** for the client's repo will be added so one `git push` updates both (see Git & Deploy).
**Languages:** Arabic (primary — Saudi market, **RTL-first**) + English (secondary). ⚠️ This flips Sky Amman's default (which is English-default).
**Theme:** _Decision pending._ The starter kit ships a light/dark **appearance toggle** (`useAppearance` hook + `settings/appearance` page) — keep, or lock to a single branded theme like Sky Amman did. Decide before heavy UI work.

---

## Reference Project — Sky Amman

We previously shipped **Sky Amman** (real-estate site) on essentially this same stack. It lives at `c:\Users\sabba\Desktop\projects\sky-amman\`. **When in doubt about a pattern** (locale middleware, `HandleInertiaRequests` shared props, Turnstile wiring, admin layout, Site Content CRUD, security headers, Cloudflare `trustProxies` CIDRs, SSR sidecar, CI), read the corresponding file in Sky Amman first — those patterns are proven.

Sky Amman's `CLAUDE.md` is large — read targeted sections via Grep / `offset`+`limit`, never the whole file.

### ⚠️ Differences from Sky Amman — don't blindly copy

This project is the **official Laravel React starter kit**, scaffolded fresh; Sky Amman was a hand-rolled setup. Concrete divergences to respect:

- **Inertia is now v3 here too** (`inertiajs/inertia-laravel` v3.1.0, `@inertiajs/react` ^3.0) — _formerly a divergence (scaffold shipped v2), resolved by the v2→v3 upgrade on 2026-06-28._ It now **matches Sky Amman (v3)**, so Sky Amman's Inertia patterns **port cleanly**: the 419/CSRF auto-reload listener is `router.on('httpException', …)` (v2's `invalid` was renamed), and the `resolvePageComponent` `.default` unwrap is **already applied** in `app.tsx` + `ssr.jsx` (v3 dropped auto-unwrap of the page module's default export). Note retab uses lowercase `./pages/` paths, not Sky Amman's `./Pages/`.
- **Lowercase, kebab file layout:** `resources/js/pages/` (not `Pages/`), `resources/js/layouts/`, `resources/js/components/` (+ shadcn-style Radix UI primitives under `components/ui/`), and **modular route files** (`routes/auth.php`, `routes/settings.php`, `routes/web.php`). Path-adjust any Sky Amman snippet accordingly.
- **shadcn/Radix UI is the component base** here (`@radix-ui/*`, `class-variance-authority`, `tailwind-merge`, `tailwindcss-animate`) — Sky Amman hand-built its components.
- **No bilingual/i18n, Turnstile, CMS, or admin** exist yet — those are Sky Amman patterns to **port deliberately**, not assume.

---

## Background — migrating off Zid

The store is **already live and selling on Zid** (hosted SaaS). We are rebuilding it on Laravel to own the codebase and add custom touches. Implications to keep in mind:

- **Data migration:** product catalogue, categories, customers, and order history likely need exporting from Zid and importing here. Plan an import path (CSV/API export → seeders or a one-off importer) before go-live.
- **Payments are ours now.** Zid bundled checkout + payment. For KSA we'll need a local gateway — **Moyasar / Tap / HyperPay** (mada + cards) and optionally **Tabby / Tamara** (BNPL). This is a **key pending decision** and shapes the orders/payments schema.
- **Feature parity first, enhancements second** — match what the Zid store does before layering "our touch."

---

## E-commerce Requirements & Decisions (client brief — 2026-06-29)

> Captured from the client brief. **Reference implementation:** `c:\Users\sabba\Desktop\projects\hardrock-ecom-demo\` — a mature Laravel 12 + Inertia **v2** e-commerce backend we built that ALREADY implements OTO shipping, Tamara, coupons, order activities, admin activity-log/undo, optimistic locking, roles (admin/editor), bilingual AR/EN, and notifications. Retab **adapts** these into a **tighter** build (gateway abstractions, thin controllers + service layer, explicit order state machine). Read hardrock-ecom-demo's files for proven patterns before building (e.g. `app/Services/Shipping/*`, `app/Services/Payments/*`).

### Payments — DECIDED
- **Cards via Moyasar** (mada + Visa/MC + Apple Pay + STC Pay) **+ Tamara (BNPL)** **+ bank transfer** (manual transfer to the store's **Al Rajhi** IBAN, **admin-verified** — added 2026-06-30 to match the client's current site). **No COD, no cash** — every order is prepaid (online gateway or verified bank transfer).
- **Store entity + bank** (from client): legal name **شركة مصنع رطاب الوطن للتمور**; Al Rajhi IBAN `SA9780000145608010008130` (acct `145608010008130`). Stored in `settings` (`bank_*`, `legal_name`); the IBAN is a public receiving account (fine to display).
- Both sit behind a `PaymentGateway` interface (mirror hardrock's `ShippingGateway` pattern); Tamara reuses hardrock's `TamaraClient`.
- **Capture model — DECIDED (hybrid per method):**
  - **Cards (Moyasar) = immediate capture** at checkout; **refund** on customer-cancel or admin-reject. Deliberately avoids any Moyasar delayed-capture dependency.
  - **Tamara = authorize at checkout, capture on admin confirmation**; **void** the authorization if cancelled/out-of-stock (Tamara supports auth→capture natively).
  - Both expose `authorize / capture / void / refund` via the `PaymentGateway` interface; the order flow calls the right one per method + state.
  - SLA: admin confirms within ~24–48h (Tamara auth expires); dashboard flags orders nearing auth-expiry.
  - ⚠️ Trade-off: rejected **card** orders incur a real refund (fee + "money in/out" UX) — acceptable while out-of-stock rejections stay rare (depends on inventory accuracy / أسماك sync); revisit (cards→delayed capture, if Moyasar supports it) if they become common.

### Shipping — DECIDED
- **OTO (Tryoto) aggregator** for fulfillment (reuse hardrock `ShippingGateway`→`OtoGateway`/`OtoClient` + `OtoWebhookController`). **GCC countries only. No Aramex, no DHL** (filter out).
- **Customer pays a SINGLE FIXED flat shipping price** across all GCC, regardless of OTO's actual carrier cost (DECIDED — client accepts absorbing the cross-GCC cost difference). One configurable number in admin.
- OTO **does** return live per-carrier rates via `checkOTODeliveryFee` (POST origin/destination/weight; auth = refresh-token→access-token, cached). Used **internally** for cost/carrier selection — NOT shown as the customer price.

### Products & Inventory — DECIDED
- **Inventory tracked by quantity (units in stock).** Weight is **descriptive only** — shown in the product title/description per product, NOT a structured field (shipping is flat, so weight never drives cost).
- **No variants (DECIDED)** — each sellable item = one product = one SMACC SKU (keeps the Excel import 1:1). All stock is unit-quantity on the product.

### Coupons — DECIDED (+ one POSTPONED)
- Admin-controlled coupon system (reuse + extend hardrock's `Coupon`).
- **Review reward — BUILT 2026-07-29** (a second earn-a-coupon path alongside the 5-purchases loyalty reward): a verified buyer who posts a review **with a comment** earns a **one-time** percentage coupon (admin picks 10% or 20%), account-bound, valid 30 days. `App\Services\ReviewRewardService` mirrors `LoyaltyService`; **off by default** and managed on `/admin/discounts` (`review_reward_enabled` / `review_reward_percent` settings). Once-ever per customer is enforced by the `loyalty_rewards` unique key `(user_id, type='review', threshold)` — ⚠️ `threshold` is meaningless for this type but the key needs a value, so it's stored as `0`. Issuing is row-locked + idempotent (safe on the review-submit path). A **post-delivery WhatsApp nudge** (`SendReviewReminder`, dispatched with a 1-day delay from `ShippingService` on delivery) invites the review; eligibility is re-checked **at send time** and `orders.review_reminder_sent_at` stops a repeated "delivered" webhook re-sending it.
- ⏸ **POSTPONED — QR-code coupons redeemable at the PHYSICAL store** (client used to issue a QR after 5 purchases for an in-store discount). Design later: signed **single-use** token, atomic redemption (optimistic locking), validated either by أسماك or a standalone cashier "redeem" web page (decoupled from أسماك). Revisit after core build.

### Order flow (client's required flow)
1. Customer orders → pays (prepaid) → payment confirmed.
2. Admin notified via **email + WhatsApp + admin-panel notification**.
3. Admin/editor checks inventory & issues, then **confirms**:
   - **If unavailable:** button to WhatsApp the customer (apologize + suggest similar products + "back soon"); the event is logged/notified + stored for **analytics** + shown on the dashboard.
   - **If confirmed:** deduct stock → alert OTO to pick up → WhatsApp customer "confirmed, courier coming."
4. **Cancellation:** customer may cancel **only before** admin confirms → notify customer **indirectly** (don't incentivize cancelling).

### Returns & exchanges — policy from current site (to replicate)
Source: retabstore.com policy. **Defect/damage-only** (matches food/perishable norms):
- **Eligibility:** ONLY for product **defects/damage** (عيوب / تمور تالفة). Excludes late receipt, shipping-company faults, and used/tampered products.
- **Window:** request within **3 days** of receiving the product → requires tracking **`delivered_at`** on orders.
- **Channel (current):** **WhatsApp** (returns dept +966 50 384 5356) with **photos** + order number + contact details.
- **Condition:** returned items in good condition with **original labels + packaging**.
- **Shipping fees:** **non-refundable** EXCEPT when the dates arrived **damaged**.
- **Resolution:** after inspection, exchange or **refund within 14 days**.
- **Build implication — returns module:** model as a **separate entity** (not just an order status): `return_requests` (order_id, user_id, status [requested/approved/rejected/exchanged/refunded], reason, resolved_at, admin_notes) + `return_items` (partial returns) + attached **photos** (media). Enforce 3-day window via `delivered_at`; refund via the payment gateway (card→Moyasar, Tamara→Tamara); shipping fee refunded only if damaged. Customer files in-account (with photos) → admin reviews → resolves; mirror WhatsApp touchpoints.
- **Also implies:** a **content/legal pages** capability (bilingual) for the policy page itself + about/contact (port Sky Amman's Site Content CRUD pattern).

### WhatsApp + Marketing — DECIDED: direct Meta Cloud API
- Use the **direct Meta Cloud API** (no BSP) — lowest cost, full control, under the same Meta Business Manager as the client's ads. We build the integration in Laravel (send + webhooks for delivery/read status).
- **Message types:** order confirmation / apology+suggest / "courier coming" / loyalty = **Utility** templates; monthly offers = **Marketing** templates (**opt-in required**). All business-initiated messages outside a customer's 24h reply window MUST be **Meta-approved templates** (free text only inside the 24h window).
- **Admin "marketing" section = two parts:** (a) **template manager** — compose + submit to Meta + track approval status (or manage templates in Meta Business Manager initially); (b) **campaign sender** — pick an approved template → fill variables → send to the opt-in segment → track delivery via webhook.
- **🔭 FUTURE IMPROVEMENT (deferred — auto-sync template approval status).** Today the template's "Meta approval status" (`draft/pending/approved/rejected` on `whatsapp_templates`) is a **manual mirror** of the real status in Meta Business Manager — templates are authored + approved THERE, and a human flips the flag here to match (the send guardrail `WhatsappTemplate::isApproved()` + the sender dropdown trust this flag but never verify it against Meta). If the manual step becomes error-prone, wire **Meta's Template Management API** to (a) **submit** templates to Meta straight from the `/admin/marketing` screen, and (b) subscribe to the **`message_template_status_update` webhook** so the local status flips to Approved/Rejected automatically. That removes the human sync step entirely (and the drift risk of a locally-"approved" template Meta actually rejects → per-message `failed` sends). Scope it when asked.
- **Guardrails:** marketing only to opted-in contacts; plan for Meta's template-approval lag; respect Meta's daily conversation limits + quality rating.
- Synergy: **click-to-WhatsApp ads** from the client's Meta campaigns start chats + open the free 24h window.

### Loyalty — DECIDED
- **5 confirmed purchases → 15% discount coupon**; customer notified + incentivized (WhatsApp).
- Confirmed buyers → added to the **monthly WhatsApp marketing list** (must capture opt-in consent at/after purchase).

### Customers, accounts & engagement — DECIDED
- **Account creation at checkout** (if not signed in) via any of: **WhatsApp** (phone OTP via WhatsApp), **Google** (OAuth — reuse hardrock's Socialite), **email** (email+password). Minimal at signup; user **completes profile later** (name, city, phone, email…).
- **Identity model:** `users` has **nullable** name/email/phone/password (a user may have only a phone, or only Google); a `social_accounts` table links providers to one account; `otp_verifications` for phone/WhatsApp sign-in.
- **OTP delivery = WhatsApp only (DECIDED)** — no SMS provider. Phone verification is via WhatsApp OTP; sign-in methods = **WhatsApp / Google / email**.
- **Reviews** (+ helpful votes; bilingual) and **wishlist** (per user) — reuse hardrock patterns.
- **Loyalty/points tracker:** count of confirmed purchases on the user; shown on the **customer's account** (progress to reward) AND the **admin** customer view; drives the 5→15% coupon. Count-based for v1, extensible to points-per-spend.

### POS integration (SMACC / سماك) — NO API; daily Excel export→import (DECIDED)
- Physical store's POS is **SMACC** ("سماك" = how S-M-A-C-C is pronounced), by **Arab Sea Information Systems** (`arabsea.com`) — major KSA accounting/POS/inventory/e-invoicing platform.
- ⚠️ **Arab Sea confirmed: NO API integration.** Only path = **export SMACC inventory to Excel**, then **import into our admin** to re-baseline website stock.
- **Inventory model (DECIDED):** **SMACC = ledger of record**; website = **daily-synced mirror** + soft reservations for in-flight online orders. Website stock is **advisory** between imports; the **per-order admin confirm-step is the real backstop** (stale stock → at worst an apology, never a bad fulfillment).
- 🔑 **Critical rule:** online sales MUST be reflected in SMACC before each export, or every import "refills" already-sold units → upward drift + overselling. Website generates a **daily "online sales" report** so the admin batch-adjusts SMACC, then exports.
- **Daily routine:** (1) website → today's online sales; (2) admin reflects them in SMACC; (3) export SMACC → Excel; (4) import to admin (diff/preview → apply).
- **Import design:** match rows by stable key → products store **`smacc_sku`/barcode**; **diff/preview** before apply (flag unmatched rows); transactional; logged + undoable (reuse `ActivityLogService`); idempotent.
- **Mitigate the daily-commitment risk:** "stock last synced: Xh ago" indicator + banner/alert if no import in >24h; low-stock buffer; one-drag import. **Ask Arab Sea:** can SMACC schedule/email the export? → enables auto-ingest of the file (still not live sync).
- Knock-on: staler mirror → more out-of-stock rejections → more captured-card refunds (see Payments). Daily import keeps that low.

### Admin panel & i18n — DECIDED
- **Storefront AR-first** then EN; **admin panel EN-first** then AR. Admin panel supports **dark + light themes**.

### Open decisions (blocking schema design)
1. ~~Payment capture model~~ — ✅ DECIDED (hybrid: cards immediate-capture, Tamara auth→capture; see Payments).
2. ~~POS / inventory~~ — ✅ SMACC by Arab Sea, **no API** → daily Excel export→import; SMACC = ledger of record, website = daily mirror + admin confirm-step backstop (see POS section).
3. ~~WhatsApp route~~ — ✅ direct Meta Cloud API (see WhatsApp section).
4. ~~Shipping flat rate~~ — ✅ single flat GCC rate.

---

## Tech Stack (as scaffolded — real versions)

- **Backend:** Laravel `v12.62.0`, PHP `^8.2` (starter kit pinned to v1.0.1 because latest requires PHP 8.3), PHPUnit `11.5`, Ziggy `v2.6.3`
- **Inertia:** `inertiajs/inertia-laravel` `v3.1.0` + `@inertiajs/react` `^3.0` (installed `3.5.0`) — upgraded from v2 on 2026-06-28 (see Build Progress → Construction phase). Requires only PHP 8.2, so the starter-kit-v1.0.1 / PHP 8.2 pin was never a blocker for Inertia itself.
- **Frontend:** React `19`, TypeScript `^5.7`, Vite `^6`, Tailwind CSS `^4` (`@tailwindcss/vite`), `lucide-react`, Radix UI primitives, `clsx`/`tailwind-merge`/`cva`
- **What the starter kit already gives us:** auth flow (login / register / forgot+reset password / email verify / confirm password), `dashboard`, `settings` (profile / password / appearance), an `AppLayout`, and a `welcome` landing page. **SSR entry `resources/js/ssr.jsx` + `npm run build:ssr` exist** (runtime not enabled yet).
- **`.npmrc`** holds `production=false` — **do not delete it.** The user's shell has a global `NODE_ENV=production` that otherwise makes `npm install` silently drop devDependencies. (The starter kit dodges the worst of it by listing build deps under `dependencies`, but keep the guard.)

---

## Architecture Patterns (MUST FOLLOW)

These are the conventions we're adopting (most proven in Sky Amman). Where a pattern isn't built yet, it's marked **(to build / port)**.

### Inertia.js
- Controllers use `Inertia::render('page/name', [...props])` — no API routes for the storefront/admin UI.
- Mutations use `router.post/put/delete()` with `preserveScroll`, `onSuccess`, `onFinish`.
- Forms with files: native `FormData` + `forceFormData: true`.
- Routing is server-driven via `routes/*.php` — no client-side router.
- Auth state via `usePage().props.auth.user` (shared by middleware).
- Flash messages: server `->with('success', …)` → client reads `usePage().props.flash`.

### Bilingual (Arabic-first) + RTL — **(BUILT — foundation + storefront; deferred items below)**
- **Locale = server session, default AR** (single source of truth, no localStorage): `SetLocale` middleware → `app()->setLocale()`, `LocaleController` + `POST /locale/{locale}` (whitelisted `ar|en`), `locale` shared via `HandleInertiaRequests`. `<html dir>` is set from the locale in `app.blade.php` for correct RTL on **first paint** (no flash).
- **Client i18n = `react-i18next` + `i18next`** (`resources/js/i18n/{index,ar,en}.ts`, `lng`/`fallbackLng` = `ar`) + `LanguageContext` (`useLanguage()` → toggle does `fetch POST /locale/{lang}`, updates `document.dir`/`lang`; seeded from the shared session locale). Wired into **both** `app.tsx` + `ssr.jsx`; provider wraps the whole app.
- **DB content (`_ar`/`_en` columns):** controllers ship **BOTH** locales; the client picks via **`useLocalized()`** (`resources/js/lib/localize.ts`, AR fallback) so the toggle is **instant** — no server round-trip (mirrors Sky Amman). Stored content always wins over i18n fallbacks.
- RTL: use CSS **logical properties** (`text-start`, `ms-*`/`me-*`, `ps-*`/`pe-*`), not `text-left`/`flex-end`.
- **Server-side strings (BUILT):** flash + error messages go through `__('messages.*')` (`lang/{ar,en}/messages.php`, storefront **and** admin); Laravel validation/auth/passwords localized (`lang/ar/*` mirroring published `lang/en/*`, incl. AR field-name `attributes`). Resolved in the request's session locale. ⚠️ Server-rendered strings (these + product names) reflect the locale **at request time** — they follow on the next request, not the instant client toggle.
- **Brand web font (BUILT 2026-07-02, switched to Thmanyah 2026-07-18):** the storefront brand face is **Thmanyah Sans** (the ثمانية typeface), **self-hosted** WOFF2 replacing the old bunny.net **Tajawal**. Same wiring mechanism: override the Tailwind v4 token under RTL — `html[dir='rtl'] { --font-sans: 'Thmanyah Sans', ... }` + `--font-heading: 'Thmanyah Sans'` in `app.css` (the `font-sans`/`font-heading` utilities resolve the tokens, so the override cascades everywhere; no per-component changes). WOFF2 files live in `resources/fonts/` (weights Regular/Medium/Bold/Black), declared via `@font-face` with **`font-display: swap`**, fingerprinted by Vite (hashed URLs, `Vite::asset()`-resolvable — the manifest keys them by `resources/fonts/<file>.woff2`), and the Regular+Bold weights are **preloaded** in `app.blade.php` (`<link rel="preload" as="font" crossorigin>` — the `crossorigin` is mandatory or the browser double-downloads). Instrument Sans stays the Latin/LTR + admin default. ⚠️ **Licence caveats (thmanyah Font Licence, KSA-governed, Arabic version prevails):** commercial website use is expressly permitted, BUT (a) the font may **not be modified/subsetted** (so no glyph-subsetting to shrink it — ship as-is, ~78 KB/weight); (b) only WOFF2 is committed, **never the OTF** (desktop-installable); (c) the licence restricts making the raw file freely downloadable ("compiled/packaged/obfuscated" web-embedding only) — the Vite hashing + same-origin serving + planned Cloudflare hotlink protection cover this; (d) committing the WOFF2 to the (private) GitHub repos is fine as "your own projects" but would be redistribution if either repo goes public; (e) **written confirmation from ask@thmanyah.com is still recommended** before launch (the licence says no exception is valid unless issued by them in writing). Licence + font files: `Downloads/Thmanyah-Font-Family` (not in repo). **SSR locale (BUILT):** `ssr.jsx` calls `i18n.changeLanguage(page.props.locale)` before render.
- **Deferred:** **admin panel i18n** only (admin UI is EN/LTR-pinned by design — server flash already localizes).

### File Storage — **(BUILT — media layer + product images)**
- **Single upload path = `App\Support\Media`** (`storeImage()` / `url()` / `delete()`): validates extension AND MIME, randomizes the filename (UUID), stores public on the **`media` disk**. **Never write public files directly** — always go through `Media` (same rule as `Setting::set()`).
- **Storage provider — DECIDED: Cloudflare R2** (S3-compatible, **zero egress fees**, fits the planned Cloudflare stack). Wired as the `r2` disk in `config/filesystems.php` via Laravel's **s3 driver** (`region: auto`, path-style endpoint) — no R2-specific code, so swapping to real S3 is an env change. The active disk is chosen by **`MEDIA_DISK`** env: defaults to the local **`public`** disk in dev (no creds; run `php artisan storage:link` once), set **`MEDIA_DISK=r2`** in production (Railway's FS is ephemeral — uploads MUST NOT live on the container disk).
  - Prod env (Railway only, never commit): `MEDIA_DISK=r2`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET`, `R2_ENDPOINT` (`https://<account-id>.r2.cloudflarestorage.com`), `R2_URL` (public bucket / custom domain).
  - **Cost:** R2 free tier = **10 GB storage + 1M writes + 10M reads per month**, then **$0.015/GB-month**, **$4.50/M writes**, **$0.36/M reads**, **$0 egress**. A dates catalogue (hundreds of images, ~sub-GB) sits in the free tier indefinitely; egress being free is the reason it beats S3 for an image-heavy storefront.
- **Product images BUILT** on this: `product_images` (path/alt/sort/is_primary) managed via `Admin\ProductImageController` (upload/delete/set-primary on dedicated multipart-POST endpoints — separate from the product text form to avoid PUT-multipart issues); first image auto-primary; deleting the primary promotes the next. Storefront catalogue/product + admin list/edit render via `Media::url()` with a 🌴 placeholder fallback.
- **Return-request photos BUILT** on this layer (`returns/{order_id}/…`, shown in the admin review). **Still pending:** review photos. Seeded/structural images (logo, etc.) still go in git under `public/`.
- **Responsive WebP variants BUILT (2026-07-22).** Every image stored through `Media` auto-generates small WebP variants so listings load a few KB instead of full-res originals (measured: a 92 KB JPEG → **15 KB** `card`, **3 KB** `thumb`). `intervention/image` v3 (GD driver, needs PHP GD **with WebP**). Sizes in **`config/media.php`** (`thumb` 150px / `card` 500px / `detail` 1400px + a `variants_enabled` master switch + `image_driver`). **`App\Support\ImageVariants`** does `generate()` (best-effort, never throws — a resize failure must not break an upload), `variantPath()` (convention: `products/1/uuid.jpg` → `products/1/uuid-card.webp`, **derived not DB-stored**), and `delete()`. Wired into `Media`: `storeImage()` generates on store, `url($path, $variant)` swaps to the variant (falls back to the original when disabled/unknown so nothing 404s), `delete()` removes variants too. Served per context: catalogue/home card → `card`, search typeahead → `thumb`, product page → `detail` (full original kept for a future click-to-zoom). Backfill for pre-pipeline images: **`php artisan media:variants`** (idempotent, `--force`) — already run for all 163 product images. ⚠️ Convention-based URLs mean a variant must exist, so **run the backfill once after enabling** (or new images 404); the `variants_enabled` switch is the safety valve, and **production PHP must ship GD/Imagick with WebP** (Railpack/FrankenPHP does). Reusable pattern captured in memory (`image-variant-pipeline`) for other storefronts.

### Change Log + Revert — **(BUILT 2026-07-06 — v2 semantics)**
- **What:** staff audit history + per-entry **Revert** at `/admin/change-log`, backed by the existing `activity_logs` table + v2 columns `old_data`/`new_data`/`label`/`reverts_log_id` (migration `2026_07_06_100000`). Code: **`App\Services\ChangeLog\ChangeLogService`** (+ `RevertResult`); wired into **Products** (created/updated/deleted — revert doubles as the only restore-from-trash path), **Settings** (one entry per save, changed keys only), **Content pages** (update revertable; create audit-only — no delete route exists). **SMACC imports** share the table but keep their bespoke `changes` payload + own undo on the Inventory page (audit-only here).
- **Design — fixes Sky Amman's catalogued failure modes** (sky-amman CLAUDE.md → "ChangeLog / Revert semantics — compound-edit behavior catalog"): (1) **update snapshots are dirty-fields-only**, so reverting an old edit can never clobber newer edits to other fields; (2) **per-field conflict detection** — every field must still hold the entry's `new_data` value or the revert refuses with the diverged field list (also kills out-of-order resurrection); (3) **transactional reverts + honest `RevertResult` reason codes** — never a "success" that did nothing; (4) **reverts are first-class entries** — a revert writes a new semantically-typed row (`updated`/`deleted`/`restored`) linked via `reverts_log_id` and stamps the original ⇒ complete timeline and **redo = revert the mirror entry** (same machinery).
- **Deliberately NOT tracked:** orders/returns/payments (state machines with their own append-only `order_activities` audit — snapshot write-backs must never bypass them) and product images (separate media flow).
- **Usage:** updates pass `$before = $model->attributesToArray()` captured pre-save to `logUpdated()`; each write+log pair runs in `DB::transaction`. Adding a tracked section = call the right `log*` method + add the subject to `REVERTABLE`/`SUBJECT_LABELS` in the service.

### Coming Soon + Demand Capture — **(BUILT 2026-07-22)**
- **Purpose:** turn incomplete draft products (the Zid import left 22 hidden as `is_active=false`) into a demand funnel instead of dead weight. A draft can be surfaced on the store as **"Coming Soon"** — visible + describable but **not buyable**; customers tap **"I want this"** to register interest, which notifies staff for WhatsApp follow-up.
- **State model — the key rule:** `products.is_coming_soon` (boolean) is a *display* flag, NEVER a buy flag. **Buyability stays gated purely by `is_active` everywhere** (cart/checkout unchanged — `CartController` still `Product::where('is_active', true)->findOrFail(...)`, so a coming-soon item 404s the cart). `Product::isComingSoon()` = `!is_active && is_coming_soon`. `scopeVisibleOnStore()` = `is_active OR is_coming_soon` and is what the **storefront catalogue/detail** query on (replacing the old bare `is_active` filter); best-sellers / new-arrivals / Offers stay `is_active`-only. `card()` ships a `coming_soon` bool → the card shows a "Coming Soon" badge + "I want this →" instead of price; the product page hides price/Tamara/add-to-cart and renders the request control.
- **Request flow:** `POST /products/{slug}/request` → `ProductRequestController` (storefront, `throttle:5,1`, **guests allowed**). Guests supply a phone + pass **Turnstile** (reuses `TurnstileVerifier` + the `turnstileSiteKey` shared prop, mirrors `OtpAuthController`); signed-in users are one click (their account carries the contact). `abort_unless($product->isComingSoon())` so the button can't be replayed against live/plain-hidden products. One open (unhandled) request per customer per product is enforced (collapses double-taps). Rows land in **`product_requests`** (`product_id`, nullable `user_id`, `phone`, `ip`, `handled_at`); staff are notified via `ProductRequestedNotification` (database channel → bell, like `ReturnRequestedNotification`).
- **Admin surfaces:** (1) **Drafts workspace** — the Products list gained a `status` filter (`active`/`draft`/`coming_soon`) with a live draft count + per-row **completeness badges** (`needs_price`/`needs_image`/`needs_description`) so staff see what each draft still needs; the product form has the **"Show on store as Coming Soon (request only)"** toggle. (2) **Dashboard** — a `draftsToComplete` action-queue item (count of `is_active=false`) → `/admin/products?status=draft`. (3) **Dedicated page** `/admin/product-requests` (`ProductRequestController@index` + `markHandled`, gated by the **new `product_requests` permission** section in `App\Support\Permission` — view/manage; ⚠️ existing editors won't have it until an admin grants it, admins bypass) — list of requests (product, account/guest phone, when) filterable open/handled + a sidebar entry.
- **Descriptions:** the 22 inactive drafts were given draft bilingual descriptions (client refines); price/photo for the zero-price / photo-less ones remain client-supplied. Only-empty fields were filled (the importer's `name_en`/`description_*` guards keep them safe on re-import).
- **Known minor:** a coming-soon product's `Product` JSON-LD still emits `price: 0.00` (transient; suppress the offer block if Google flags it).

### Admin UI conventions — **(BUILT 2026-07-28/29 — use these, don't re-roll them)**
The admin panel got a consistency pass; these are now the shared primitives. **Reach for them before hand-rolling a badge, a toggle, or a flash message.**
- **Status tags = `resources/js/components/status-pill.tsx`** — one soft-tint pill (tinted fill + inset ring + leading dot) with a fixed `StatusTone` palette (`neutral|amber|blue|indigo|green|red|purple|teal`), dark mode using translucent `…/10` tints so pills don't glow. Every ad-hoc badge sitewide (order status, payment status, coupons, returns, reviews, change-log, product-requests, content-pages, customers) routes through it. New states pick an existing tone — **don't add bespoke badge classes.**
- **One-click state flips = `components/admin/status-toggle.tsx`** — a `StatusPill` that doubles as a toggle button (product visibility, coupon active), fires `router[method](url)` with `preserveScroll/preserveState`, shows a busy state, controller back-redirects to refresh the row. **No confirm step** (the flip is trivially reversible). ⚠️ When blocked it uses **`aria-disabled`, not the native `disabled`**, so the button stays hoverable and the `hint` tooltip can still explain *why*.
- **Flash = overlay toasts (`components/admin/admin-toasts.tsx`)**, not an in-flow banner (the old banner shifted the page on every save). Pinned top inline-end (top-left in RTL), success auto-dismisses after 4 s (hover pauses), **errors linger until closed**, stack capped at 4. ⚠️ **Gotcha:** the effect keys on a **per-response `flash.id` nonce** added in `HandleInertiaRequests` — without it, two consecutive actions flashing the *same* string wouldn't re-fire (an identical value doesn't change a value-compared effect dep), so the second toast silently never appeared. Any new flash surface must keep that nonce.
- **List pagination = `Controller::perPage($request, $default)`** (`app/Http/Controllers/Controller.php`) + `components/admin/pagination.tsx`. Page size comes from `?per_page=N` **whitelisted against `PER_PAGE_OPTIONS` [10,25,50,100]** (guards arbitrary/huge page sizes); a caller default outside the list is merged in by the UI as an extra option. Wired on products / orders / customers / client-reviews / product-requests.
- **Per-user view preferences:** the products card⇄table toggle persists in **`localStorage`** (`admin.products.view`) — a pure display preference, deliberately not a server round-trip. Column widths still use the server-side `ui_preferences.tableWidths`.
- **Media viewing:** `components/image-lightbox.tsx` (full-screen viewer) + `components/store/product-gallery.tsx` (storefront hover-zoom) are shared; the admin product image opens the same lightbox for zoom/swap. Thumbnails must request the **`card`** variant, not the original (see File Storage → variants).

### Notifications & alerts (staff) — **(bell + WhatsApp BUILT; email + live bell added 2026-07-30)**
- **Three triggers, three channels.** New order (`CheckoutController`), return filed (`ReturnService`), coming-soon product requested (`ProductRequestController`). Each does `Notification::send(User::staff()->get(), …)` **plus** a separate WhatsApp fan-out (the first two only) to the phone list in `config('services.whatsapp.admin_recipients')`.
- **Channels per notification:** `database` (the bell) + `mail`. ⚠️ **Mail is conditional per recipient** — `via()` returns `['database']` alone when `$notifiable->email` is null, because `users.email` is **nullable** under the OTP identity model and the mail transport throws on an empty address.
- **`viaConnections(): ['database' => 'sync']`** on all three: the bell row is written **in-request** (instant, and it keeps working even with no queue worker), while only the email goes to the queue — off the customer's checkout request. Framework support is real (`NotificationSender` honours `viaConnections`/`viaQueues`).
- **Admin emails are pre-rendered ENGLISH literals, not `__()`** — deliberate. The admin's panel language lives in their browser (localStorage + the `admin_locale` cookie), so a notification triggered by a **customer's** request has no way to know it, and a queued notification would otherwise capture the customer's AR session locale. The bell payload stays structured/client-rendered for exactly the same reason.
- **The bell is live without websockets:** `router.poll(45s, { only: ['notifications'] }, { keepAlive: false })` — an Inertia **partial** reload, so the server recomputes only the `notifications` closure (two indexed queries) and scroll/state are preserved for free; a hidden tab is throttled to one poll in ten ticks. Deliberately polling, not broadcasting — no Reverb/Pusher service to run. Plus a tab-title unread badge and an optional chime (WebAudio, no asset to ship; muted per browser under `admin.notifications.sound`).
  - ⚠️ **Gotcha — Inertia owns `<title>`.** It rewrites the title on hydration and on every navigation, so a badge written once (even one frame late via `requestAnimationFrame`) is silently dropped. The fix is a **`MutationObserver` on `document.head`** that re-applies the prefix whenever anything touches it, with an equality guard so our own write doesn't loop the observer.
- **WhatsApp transport is QUEUED** — `App\Jobs\SendWhatsappMessage` (`tries = 3`, backoff 30/120/600s). The ledger row is written synchronously as `queued` (so the admin log shows the attempt immediately) and only the Meta call is deferred; the job flips it to sent/failed and is idempotent (a retry after a landed send no-ops). Before this, every transactional send was an **inline HTTP call per recipient inside the customer's request** — seconds of checkout latency, and a transient failure lost the alert to a log line.
  - ⚠️ **Params are passed to the job, never read back off the row** — the ledger deliberately **redacts** sensitive params (OTP codes), so the stored payload is not a faithful send payload.
  - ⚠️ **OTP sends inline** (`dispatch(..., queue: false)`): the customer is staring at the code field, and it keeps plaintext codes out of the `jobs` table.
  - ⚠️ **The job rethrows to trigger retries, EXCEPT under the `sync` driver** (`$this->job?->getConnectionName()`), where the exception would surface inside the caller's request. A Meta outage must never break a checkout — that guarantee predates the queue and is preserved.
- 🔴 **DEPLOY PREREQUISITE — a queue worker MUST run in production** (`php artisan queue:work`, a second Railway service off the same image). `QUEUE_CONNECTION=database`, so without a worker the jobs table just fills: admin **emails** and **WhatsApp messages** would never send (the bell still works — that's what `viaConnections` buys). This is **not new** with these changes: `SendReviewReminder` (the 1-day-delayed review nudge) and `SendWhatsappCampaign` already depended on it silently.
- **History page — `/admin/notifications`** (BUILT 2026-07-30): the bell only carries the latest 10, so everything older was unreachable. Paginated own-notifications list with read-state + type filters, reached via **"View all"** in the dropdown (no sidebar entry — notifications belong to the navbar bell). Rows reuse the bell's open route, so clicking still marks read + redirects to the subject.
  - **Payload→text rendering is shared**: `components/admin/notification-content.tsx` (`useNotificationText()` + `iconFor()` + `NOTIFICATION_TYPES`), imported by BOTH the bell and the page, so a new notification type can't render in one and not the other. A hook rather than plain functions only so callers don't pass `t` around.
  - ⚠️ **Gotcha — the page paginator is called `entries`, NOT `notifications`.** `notifications` is the **shared bell prop** (`{unread, items}`); a page prop of the same name silently overrides it, and the bell's `items.filter(...)` would then throw on this very page. Pinned by `NotificationBellTest::test_history_does_not_shadow_the_shared_bell_prop`.
  - Type filter uses a JSON-path where on the structured payload (`where('data->type', …)`, whitelisted so it's never raw input) → `json_unquote(json_extract(...))`. **Verified on SQLite (tests) AND MariaDB 10.4 (dev)**; MySQL 8 (prod) is strictly better.
- **Mail provider = Resend (WIRED 2026-07-30).** `resend/resend-php` installed (the framework's `ResendTransport` needs it for `Resend::client()`); `config/mail.php` already had the `resend` mailer and `config/services.php` the `RESEND_KEY` binding, so no config changes were needed. Verified with a real send to Resend's simulator (`delivered@resend.dev`).
  - **The client's key is SEND-ONLY (restricted)** — good practice, but it means it can't read any other endpoint. `GET /domains` answers **401 `restricted_api_key`**, which `App\Support\ResendProbe` treats as **OK** (an invalid key answers **400 `validation_error`** instead, and a full-access key answers 200 with each domain's verification status — three cleanly distinguishable states). Reported by `integrations:check` alongside the payment providers.
  - 🔴 **Remaining step: verify `retabstore.com` in Resend** (add the domain, paste DKIM/SPF into Cloudflare DNS). Confirmed empirically — sending from `no-reply@retabstore.com` today fails with *"The retabstore.com domain is not verified"*. **Until then the only accepted from-address is `onboarding@resend.dev`**, which is what `.env` uses.
  - **Dev deliberately stays on `MAIL_MAILER=log`.** The seeded dev staff use fake `@retab.com.sa` addresses, so pointing dev at Resend would bounce them off a brand-new account and dent its sending reputation. `php artisan mail:test <address>` sends through the **resend** mailer explicitly (`--mailer=` to override), so a real send never requires flipping the global default. Production sets `MAIL_MAILER=resend`.
  - ⚠️ Mail is **queued** (see `viaConnections` above), so nothing sends without the worker.
- **Not built yet** (ranked next steps): **customer-facing email** (there is still none at all — most painful for bank transfer, where the IBAN and order number live only on one page view); alerts for **Tamara authorizations nearing expiry** (money — auths expire in 24-48h; the dashboard already computes `tamaraExpiring`, so it's a scheduled command away — but it would be the project's FIRST scheduled task ⇒ a `schedule:work` service) and low-stock / sync-gone-stale; **per-recipient routing** (admin phones/emails are env-only today, and every staff member gets everything).

### Auth & Security — **(harden before launch)**
- Session-based auth (starter kit default). Use `Auth::id()`/`Auth::user()`, never the `auth()` helper in new code (Sky Amman convention).
- Rate-limit all public POST endpoints; add a bot gate (Cloudflare Turnstile) on public forms (port from Sky Amman).
- Server-side validation on ALL endpoints; validate file types by extension AND MIME; parameterized queries / Eloquent only.
- Add a `SecurityHeaders` middleware (CSP allowlist, Permissions-Policy) — port from Sky Amman, skip CSP in local dev.
- Production HTTPS behind a proxy: `URL::forceScheme('https')` when `APP_ENV=production` + correct `trustProxies` CIDRs (see Sky Amman's `bootstrap/app.php`).

### SEO — **(BUILT — sitemap/robots/JSON-LD 2026-07-02; 301s + slug cleanup 2026-07-25)**
- Storefront SEO matters (organic product discovery). Server-render public pages (enable SSR — see Build Progress), per-page/per-product `<Head>` meta + OG, dynamic `sitemap.xml`, `robots.txt` disallowing admin, JSON-LD (`Product`/`Offer`/`BreadcrumbList`), and `hreflang` AR↔EN. _(hreflang deliberately skipped — session-based locale shares one URL per language; see the 2026-07-02 Build Progress entry.)_
- **301 redirects from the old Zid URLs + slug cleanup — BUILT 2026-07-25 (run `catalog:clean-slugs --apply` on prod AT LAUNCH).** _Why it matters:_ the live Zid store is already indexed by Google; each ranked result points at an old Zid slug. Zid used the **same** `/products/{slug}` path we do (verified: `retabstore.com/products/{old-slug}` → 200), and the importer seeded our `products.slug` **from** `old_zid_slug` — so today the old URLs already resolve. The moment we clean a slug, its old URL would 404 and the ranking (link equity) is lost. _What was built:_
  - **`redirects` table** + `App\Models\Redirect` (`from_slug` unique, nullable `product_id` FK, static `to_url` fallback, `status` default 301, `hits`). `target()` resolves to the product's **current** slug (so a later re-clean still lands right) or the static URL, else null → 404.
  - **`catalog:clean-slugs {--apply}` command** — dry-run by default. Regenerates only **junk** slugs (ar-prefixed / English / uppercase / leading-hyphen — detected as "contains a Latin letter or leading `-`") into clean **Arabic** slugs from `name_ar` via **`App\Support\ArabicSlug`** (preserves Arabic + Arabic-Indic digits, strips tatweel/harakat, lowercases Latin; Str::slug would transliterate Arabic away). Already-clean Arabic slugs are **left untouched (no redirect)** → fewest redirects, best ranking preservation. Records a 301 from each changed slug; transactional; idempotent (cleaned slugs aren't junk on re-run; `firstOrCreate` redirects); dedupes collisions with `-2`. On the real catalogue: **27 of 87** slugs cleaned.
  - **Resolution:** the `/products/{product:slug}` GET route has a **`->missing()`** hook → `RedirectController@missingProduct` (301 to `target()`, increments `hits`, else 404). Non-breaking (keeps the `{product:slug}` binding; `route('shop.product', …)` still generates by slug) and **`route:cache`-safe** (the missing closure serializes via `SerializableClosure` — verified). Chose Arabic slugs over English (client decision 2026-07-25).
  - **Launch step:** after final slugs are settled, run `php artisan catalog:clean-slugs` (preview) then `--apply` **on prod**. Future legacy paths (e.g. old category URLs) can be added as manual `redirects` rows (set `to_url`). _Deferred:_ the 16 Zid **variant-parent** URLs (indexed but not 1:1 with a product) — add hand-mapped `redirects` rows if they show in Search Console.

### Deployment + SSR
- Likely Railway (Railpack, PHP 8.2 + Node) behind Cloudflare — port Sky Amman's startup (`migrate --force` → `storage:link` → `optimize`).
- Use `SESSION_DRIVER=database` in production (ephemeral FS).
- **SSR:** the starter kit ships `resources/js/ssr.jsx` + `npm run build:ssr`. To actually enable it we still need to: publish `config/inertia.php` (env-driven `INERTIA_SSR_ENABLED`), add a timeout/graceful-fallback HTTP gateway (so a hung SSR process falls back to CSR instead of 502-ing), and stand up a production SSR sidecar. **Port Sky Amman's `app/Ssr/TimeoutHttpGateway.php` + sidecar setup.** (Sky Amman docs two gotchas: Railpack pruning Node from the runtime → `RAILPACK_DEPLOY_APT_PACKAGES=nodejs`; and the SSR URL host must match the private domain exactly + is baked at `config:cache` time.)

---

## Database Schema (BUILT — schema v1 complete on `construction_phase`)

> **Source of truth = the migrations** in `database/migrations/` (`2026_06_29_1000xx_*`) + Eloquent models in `app/Models/`. Migrated clean on **MariaDB (dev)** and **SQLite (tests)**; 26 tests green. Conventions: bilingual `_ar`/`_en` (AR required, EN nullable, app falls back to AR); quantity `stock` (no variants); `smacc_sku` import key; soft-deletes on `products` + `users`; money `decimal(10,2)` SAR; **order/payment/coupon/return state are backed enums** in `app/Enums/`.
>
> **Tables by domain:**
> - **Catalog:** `categories`, `products`, `product_images`
> - **Accounts:** `users` (extended: phone, role, locale, admin_theme, whatsapp_opt_in, confirmed_purchases_count; name/email/password now nullable), `social_accounts` (Google/OAuth), `otp_verifications` (WhatsApp OTP), `addresses` (GCC)
> - **Cart:** `carts`, `cart_items`
> - **Orders:** `orders` (state machine via `OrderStatus`; `delivered_at`; OTO + payment fields), `order_items` (snapshots), `order_activities` (append-only audit)
> - **Payments:** `payments` (auth/capture/void/refund ledger)
> - **Coupons/Loyalty:** `coupons` (admin-controlled; `channel`/`source`), `coupon_redemptions`, `loyalty_rewards` (5 buys → 15%)
> - **Returns:** `order_returns` (defect-only; photos; `ReturnStatus`), `return_items`
> - **Engagement:** `reviews`, `review_helpful_votes`, `wishlists`
> - **Platform:** `content_pages` (bilingual CMS), `whatsapp_messages` (Cloud API log), `notifications` (Laravel bell), `demand_events` (unavailable analytics), `settings` (`Setting::get/set`), `activity_logs` (admin audit + revert — change-log v2, see Architecture → Change Log)
> - **Enums (`app/Enums/`):** `OrderStatus` (+enforced transitions), `PaymentStatus`, `PaymentMethod`, `CouponType`, `PaymentTransactionType`, `ReturnStatus`
>
> **Deliberately NOT built (schema-ready, deferred):** product variants (1 product = 1 SMACC SKU); QR in-store coupons; the payment/shipping/WhatsApp **service** layer (gateways/OTO/Cloud-API clients) — that's the next phase.

_Original roadmap (superseded by the BUILT inventory above — kept for historical context):_

- `products`, `categories` (+ pivot), `product_variants` (size/weight/grade — relevant for dates), `product_images`
- `inventory` / stock tracking, `prices` (incl. sale price), maybe `coupons`/`discounts`
- `carts` + `cart_items` (guest + logged-in), `orders` + `order_items`, `order_status` history
- `customers`/extended user profile, `addresses` (KSA address format), `payments` (gateway txn refs), `shipping`/`fulfillment`
- Bilingual product content (AR/EN) — decide column-per-locale vs. a translations table early.

> **Document every real schema decision here as it's made**, Sky-Amman-style (the "why", not just the "what"), so future agents don't re-litigate it.

---

## Key File Locations (as scaffolded)

```
routes/web.php, auth.php, settings.php, console.php   → route definitions (modular)
resources/js/app.tsx            → client entry
resources/js/ssr.jsx            → SSR entry (build-time only until runtime is wired)
resources/js/pages/             → page components (welcome, dashboard, auth/*, settings/*)
resources/js/layouts/           → AppLayout + auth/settings layouts
resources/js/components/         → shared components (+ ui/ shadcn-Radix primitives)
resources/js/hooks/             → e.g. useAppearance
resources/js/lib/               → utils (cn, etc.)
resources/js/types/             → shared TS types
resources/css/app.css           → Tailwind v4 entry + theme tokens
database/migrations/            → starter-kit defaults only so far
.npmrc                          → production=false (keep — NODE_ENV gotcha)
```

---

## Code Quality Rules (adopted from Sky Amman)

- If a model has a dedicated method (`Media::storeFile()`, `Setting::set()`), **always use it** — never bypass with raw `::update()`/`::create()`.
- The same write (create/update/restore) must go through one shared model method.
- **Never query inside a loop** — batch-fetch with `whereIn()`/`pluck()` first. Paginated views query related data only for the current page's IDs.
- Guard no-op operations (short-circuit when nothing changed).
- If a method returns a meaningful value (bool/count/status), use it — don't call-and-ignore.
- Match the surrounding code's style, naming, and comment density.

---

## Local Development

- **Start (all-in-one):** `composer run dev` — runs `php artisan serve` + queue listener + `npm run dev` concurrently.
- **Or separately:** `php artisan serve` + `npm run dev`.
- **URL:** `http://localhost:8000`
- **Database:** XAMPP **MariaDB 10.4.32** on `127.0.0.1:3307`, db `retab-stores` (user `root`, empty password) — set in `.env`. Reset: `php artisan migrate:fresh --seed`. ⚠️ `php artisan db:show` errors on MariaDB (queries `performance_schema.session_status`, which it doesn't expose) — cosmetic only; use `migrate:status` / `db:table <name>` to inspect.
- **Build:** `npm run build` — emits the client **and** the SSR bundle (`build:ssr` is now an identical alias, so Railway's default build produces the sidecar bundle without extra config).
- **Backend tests:** `php artisan test` (PHPUnit, in-memory SQLite).
- **Mail in dev:** default `MAIL_MAILER=log` writes to `storage/logs/laravel.log` — no provider key needed.
- **`.npmrc` guard:** if `npm install` ever drops vite/devDeps, `.npmrc` (`production=false`) should prevent it; fallback `npm install --include=dev`.
- **⚠️ Composer gotcha — `composer update <package>` can silently install a DEV branch and "fix" nothing (hit 2026-07-30).** _Symptom:_ `composer update guzzlehttp/guzzle` reported `Upgrading guzzlehttp/guzzle (7.12.3 => 7.12.x-dev 9aa17bc)` — a **dev-branch commit of a production HTTP library** — and `composer audit` still listed the same 4 advisories afterwards. _Root cause (two things combined):_ (1) the starter kit ships **`"minimum-stability": "dev"`** (with `prefer-stable: true`), so dev branches are *allowed* whenever no stable version satisfies the resolution; (2) `composer update <one-package>` deliberately **will not touch that package's own dependencies** — guzzle ≥7.15 requires `guzzlehttp/psr7 ^2.13` + `guzzlehttp/promises ^2.5.1` (installed: 2.12.3 / 2.5.0), so the stable target was unreachable and composer quietly settled on the highest thing it *could* take: the 7.12 dev branch. Diagnose with **`composer why-not guzzlehttp/guzzle 7.15.1`** (names the blocking requirements). _Fix:_ **`composer update guzzlehttp/guzzle --with-dependencies`** → 7.15.2 stable + psr7 2.13.0 + promises 2.5.1, `composer audit` clean, 348 tests green. _Lesson for any security update:_ afterwards, confirm the resolved version is **stable AND above the advisory's "Affected versions" bound** — re-run `composer audit` rather than trusting that the update did something, and reach for `-W` before concluding no fix exists.
- **⚠️ Intelephense gotcha — P1005 "Expected 4. Found 2." on `Model::where(...)` (solved for good 2026-07-02, machine-level).** _Symptom:_ every static Eloquent call (`Product::where(...)`) red-squiggled with P1005, in **every** Laravel project, surviving even `intelephense.diagnostics.argumentCount: false`. _Root cause (three parts):_ (1) the **official Laravel VS Code extension** (`laravel.vscode-laravel`) generates `vendor/_laravel_ide/_model_helpers.php` in every project, redeclaring each model with **defaults-less** `@method static mixed where($column, $operator, $value, $boolean)` — class-level tags shadow the real optional-param signatures, so Intelephense counts all 4 as required; (2) a **second embedded Intelephense** was running — the `porifa.laravel-intelephense` fork bundles its own server, labels diagnostics `intelephense`, but only reads `laraphense.*` settings with **no diagnostics toggles** — that's why the global `argumentCount: false` "didn't work"; (3) the separate P1013 (`session()->push` "undefined") is a framework docblock gap — `Request::session()` advertises the Session **contract**, which lacks `push()` (only the concrete `Store` has it). _Fix (machine-wide, covers all sibling projects):_ uninstalled `porifa.laravel-intelephense`; added `**/vendor/_laravel_ide/**` to **`intelephense.files.exclude`** in global VS Code settings (kept the default excludes — the setting replaces, not merges); removed the now-unneeded `argumentCount: false` so real arg-count checking is back. _Project-level companion:_ `barryvdh/laravel-ide-helper` (dev dep) supplies the **correct** signatures + model `@property` autocomplete — `write_eloquent_model_mixins => true` puts `@mixin \Eloquent` on vendor's base `Model` (wiped by `composer update` → re-applied by the `post-update-cmd` hook); `ide-helper:models --write-mixin` needs the DB up, re-run after schema changes; `_ide_helper*.php` gitignored — regenerate after fresh clone. Contract-vs-implementation gaps (session `push`) still need a one-line inline `/** @var \Illuminate\Session\Store $session */`. After changes: fully restart VS Code.

---

## Git & Deploy

- `origin` is wired to **your** repo: `https://github.com/mercenary19961/retab-stores.git`.
- **Dual-push is ACTIVE** (configured 2026-06-28; **re-fixed 2026-07-02** — ⚠️ gotcha: the config had drifted onto a redundant remote named `upstream`, so plain `git push` — which uses `origin` — was NOT reaching the client repo while this doc said it was. _Symptom:_ client repo stays empty/stale despite pushes. _Check:_ `git remote -v` must show the 2 push URLs on **origin**, not on some other remote. _Fix:_ the two `set-url --add --push origin` lines below + `git remote remove upstream`) — one `git push` → **both** repos. ⚠️ **Second gotcha (2026-07-02): `mercenary19961` has READ but NOT WRITE on the client repo** — pushes 403 on the second URL (`Permission ... denied to mercenary19961`) while the own-repo push succeeds; VS Code's fork-dialog misleadingly names the OWN repo when this happens. The June-28 "access verified" note only ever verified READ (`ls-remote`). ✅ **RESOLVED (verified 2026-07-04):** the client granted Write, the first push landed, and the client repo is now **fully populated and in sync** with origin (`main` + `construction_phase` at identical SHAs; `git push --dry-run https://github.com/retab-dates-dev/retab-website.git construction_phase` → "Everything up-to-date"). The client repo **`https://github.com/retab-dates-dev/retab-website.git`** is the **Railway production source**. Configured via:
  ```bash
  git remote set-url --add --push origin https://github.com/mercenary19961/retab-stores.git    # re-add own repo FIRST
  git remote set-url --add --push origin https://github.com/retab-dates-dev/retab-website.git   # client / prod repo
  git remote -v   # → 1 fetch URL + 2 push URLs
  ```
  ⚠️ The **first** `--add --push` line (re-adding your own repo) is required — adding any explicit push URL drops the implicit default, so without it pushes would go **only** to the client repo. Read access to the client repo is verified (`git ls-remote` → exit 0).
- **Merge workflow — REVISED 2026-07-23: local-merge + dual-push via the `git ship` alias; NEVER use GitHub's merge button.** _History:_ the old rule (2026-07-04) was "merge via the PR button in BOTH repos" because the client repo had a **branch-protection ruleset requiring PRs to `main`** (a direct `main` push 403'd with `GH013 … Changes must be made through a pull request`). That button-in-both-repos habit is exactly what made the two `main`s **permanently SHA-divergent** (each Merge button mints its own merge commit) and produced the endless "N commits behind/ahead" churn. **On 2026-07-23 the client removed that ruleset**, so direct pushes to client `main` work again — which lets `main` have a **single writer** (your machine) and stay byte-identical on both repos.
  - **The rule now:** advance `main` by merging **locally**, then one `git push` dual-pushes the **same commit** to both repos → the two `main`s can't diverge. **Do not click GitHub's Merge button** (two independent buttons = two different merge commits = divergence returns).
  - **Tooling:** a repo-local git alias **`git ship <branch>`** (in `.git/config`, not committed) does the whole thing: `switch main → pull → merge --no-edit <branch> → push` (dual) `→ switch back → merge --ff-only main → push` the branch. It **keeps + re-syncs the branch** (branch ends at 0 ahead / 0 behind, no "behind main" churn) and **refuses to ship `main` into itself**. So the loop is: commit on a branch → `git push` (CI runs) → `git ship <branch>` once green. PRs are optional, for reading CI only — the merge never happens through the button.
  - ⚠️ **Tradeoff of removing the ruleset:** production `main` (client repo = Railway source) no longer has a server-side PR gate — anyone with write can push straight to prod `main`. The gate is now **discipline**: only `git ship` after CI is green. (Re-add a ruleset later if the client wants a hard gate; then the deploy step becomes a PR merge on the client repo again and the mains go back to harmless SHA-divergence.)
- Dual-push config (origin fetches your repo, pushes to both) — `git remote -v` must show **1 fetch + 2 push URLs on `origin`**:
  ```bash
  git remote set-url --add --push origin https://github.com/mercenary19961/retab-stores.git    # re-add own repo FIRST (an explicit push URL drops the implicit default)
  git remote set-url --add --push origin https://github.com/retab-dates-dev/retab-website.git   # client / prod repo (Railway source)
  ```
- Commit/push **only when the user asks** (don't auto-push). Branch off `main` for feature work.

---

## Commit Message Convention

Format: `type(scope): short description`

Types: `init` (scaffolding/setup) · `feat` · `fix` · `refactor` · `style` (visual only) · `doc` · `chore`

Rules: lowercase subject, no trailing period, present/imperative tense ("add" not "added"), under 72 chars, be specific ("fix: resolve 419 on locale toggle", not "fix: fix bug").

**Do NOT add any attribution/co-author trailer to commit messages** — no `Co-Authored-By:` line, no "Generated with" line. _(User preference set 2026-06-30; overrides the harness default.)_

## Collaboration — Commit Message Suggestions

After completing any task that touches code, end the reply with a **one-line suggested commit message** in the convention above (don't run the commit — just suggest it). Skip this for purely exploratory work (reading/answering) or when no files changed.

---

## Build Progress

### Scaffold (DONE — 2026-06-28)
- [x] `laravel new retab-stores --react --phpunit --database=sqlite` — Laravel 12 + Inertia v2 + React 19 + TS + Tailwind v4 (starter kit v1.0.1) — _Inertia later upgraded v2→v3, see Construction phase_
- [x] `.npmrc` (`production=false`), `npm install`, production build verified
- [x] Git on `main`, `origin` → `mercenary19961/retab-stores`
- [x] This `CLAUDE.md` (conventions + maintenance pattern established, Sky Amman set as reference project)

### Construction phase (in progress — branch `construction_phase`)
- [x] **Inertia v2 → v3 upgrade** (2026-06-28) — `inertia-laravel` v2.0.24→**v3.1.0**, `@inertiajs/react` ^2.0→**^3.0** (3.5.0). Done up front, before any feature work, so the major bump stays cheap and the project matches Sky Amman's v3. Changes: applied v3's `resolvePageComponent(…).then(m => m.default)` unwrap in `app.tsx` + `ssr.jsx` (v3 dropped default-export auto-unwrap — the one real breaking change that hit the scaffold). Blade `@inertia`/`@inertiaHead` directives unchanged (still valid in v3); no axios/qs/lodash in the scaffold so those v3 dep-removals were transparent (`npm` pruned 12 transitive packages). The PHP-8.3 concern in the docs was about the starter-kit _template_, not Inertia (v3 needs only PHP 8.2). **Verified:** `npm run build` ✓ + `php artisan test` ✓ (26 passed, 63 assertions). ⚠️ Browser-side hydration not yet smoke-tested in a real browser.

### Foundation (TODO)
- [ ] **Decisions:** theme (keep dark-mode toggle vs. single branded theme) · payment gateway (Moyasar/Tap/HyperPay + Tabby/Tamara) · **dev↔prod DB parity** (dev is XAMPP MariaDB 10.4 — old/EOL; prod planned MySQL 8 — match engines via Docker `mysql:8`, or knowingly accept the gap?) · hosting (Railway?) · mail (Resend?)
- [x] **Arabic-first bilingual + RTL** — foundation (`SetLocale` default AR, `LocaleController`, `react-i18next`, `LanguageContext`, locale-driven `<html dir>`) + **all storefront/account/auth pages localized** with an instant AR⇄EN toggle (DB content via `useLocalized`, both locales shipped). See Architecture → Bilingual. _Deferred: server-side flash/validation, admin i18n, Arabic web font._
- [x] **E-commerce schema** — built on `construction_phase`: 28 tables + 6 enums + Eloquent models, migrated on MariaDB + SQLite, 26 tests green (see Database Schema). _Service/gateway layer next._
- [ ] **Zid data migration** path (export → import catalogue + customers + order history). _Catalogue export analysed 2026-07-15 (`products_export_*.xlsx`, 103 rows = 16 variant parents + 30 children + 57 simples → **87 flattened products**; cleaned into a Google-Sheet import template `retab-products-clean.csv`). **Three migration blockers surfaced:** (1) **SMACC SKUs missing on 65/87 rows** — Zid's `Z.30547.*` SKUs are Zid-internal junk, unrelated to SMACC; the daily stock sync keys off `smacc_sku`, so the client MUST supply real codes or we match-by-name on first import; (2) **product images live on `media.zid.store` CDN and die on cutover** — must download → re-host to R2 before the store is switched off (URLs preserved in the sheet's `images` column); (3) **no real English catalogue** — `name_en`/descriptions are just Arabic copied into the EN column (AR-fallback covers it, but there's no EN content). Also: 5 zero-price drafts, ~8 odd fractional prices, and 44 "Infinite"-stock rows to sanity-check. See the 301-redirect note under Architecture → SEO._
- [x] **Security hardening** (2026-07-02, ported from Sky Amman) — `SecurityHeaders` middleware (nosniff/frame/referrer/permissions always; **CSP + HSTS skipped in local only** — Vite HMR's bracketed-IPv6 origin breaks CSP; allowlist = challenges.cloudflare.com, cloudflareinsights, fonts.bunny.net; no gateway origins needed since checkout redirects OUT to hosted pages); **trustProxies locked to Cloudflare CIDRs + RFC 1918** (never `*` — X-Forwarded-For spoofing would poison rate limits/audit IPs; refresh from cloudflare.com/ips) + `URL::forceScheme('https')` in production; **Turnstile** (`TurnstileVerifier` service — no-ops without `TURNSTILE_SECRET_KEY`, fails CLOSED on CF outage; `turnstile.tsx` widget renders nothing without the site key, shared via `turnstileSiteKey` prop) gating **WhatsApp OTP send** (each send costs a real message — the highest-abuse surface); **rate limits** on every public POST (locale 30/m, cart 60/m, checkout 10/m, reviews 10/m, helpful+wishlist 30/m, returns 5/m, register 10/m; OTP already 6/m). Prod env needed: `TURNSTILE_SITE_KEY`/`TURNSTILE_SECRET_KEY`.
- [x] Pushed to GitHub (`origin`) + **dual-push to client repo `retab-dates-dev/retab-website` configured** (Railway prod source). ✅ Client repo populated + in sync with origin (write access granted; verified 2026-07-04).

### Storefront + Admin (in progress — most of the vertical is built; checklist was stale, corrected 2026-07-02)
- [x] Storefront: catalogue, product detail, cart, checkout (Moyasar/Tamara/bank transfer), order confirmation, customer account — **built + bilingual (AR⇄EN)**
- [x] **Returns module** (2026-07-02) — defect/damage-only per policy: customer files in-account from the order page (photos via `Media`, 3-day window off `delivered_at`, one open return per order) → admin reviews at `/admin/returns` (photos + refund preview) → approve/reject, then resolve as **exchange** or **refund**. Refunds are REAL now: `PaymentGateway::refundPayment` + `PaymentService::refund` (Moyasar `POST /payments/{id}/refund`, halalas) and `TamaraService::refund` (`/payments/simplified-refund`, major units); ledger `Refund` rows carry the actual (possibly partial) amount; `payment_status` → `refunded`/`partially_refunded`; **shipping fee refunded only when goods arrived damaged** (admin toggle); bank transfer = manual, recorded only. Also retired the `OrderConfirmationService::releaseFunds` TODO — unavailable-with-captured-card now really refunds (best-effort + logged). WhatsApp: `admin_return_requested` + `return_update` templates. Flow in `ReturnService`; guard rails throw localized `messages.returns.*`.
- [ ] Admin/back-office — **partly built:** products CRUD ✓, orders (lifecycle) ✓, SMACC stock-import ✓, returns review ✓, **settings ✓** (flat shipping fee + bank details + legal name; allowlisted keys via `Setting::set`), **content/CMS ✓** (bilingual `content_pages` CRUD at `/admin/content-pages` → public `/pages/{slug}`, AR required/EN optional, `useLocalized` instant toggle; footer links + `ContentPageSeeder` seeds returns-policy/about/contact — policy text from the real store, non-destructive re-seed); **customers view ✓** (read-only directory at `/admin/customers`: search + opt-in filter, staff excluded; detail = profile + loyalty progress/rewards + last 20 orders); **WhatsApp marketing ✓** (2026-07-02, `/admin/marketing`: template registry mirroring Meta Business Manager — authored/approved THERE, status synced manually here, composite name+language unique; campaign sender to the opt-in segment via queued `SendWhatsappCampaign` chunks, guardrails = approved templates only + consented-customers-with-phone only + staff excluded; ledger rows carry `campaign_id` → per-campaign delivery funnel from the existing webhook status updates; campaign messages send in the TEMPLATE's language, not the session default. ⚠️ Gotcha: `Model::create()` does NOT hydrate DB column defaults — the `status => 'draft'` gate needed the value set explicitly). **change log ✓** (2026-07-06 — v2 audit + conflict-checked revert at `/admin/change-log`; see Architecture → Change Log + Revert). **Admin feature set is now COMPLETE.** _(Admin still EN/LTR — its i18n is later.)_ _(The old "build before page-render tests" Vite-manifest gotcha is retired — `tests/TestCase.php` now calls `withoutVite()`.)_
- [x] **SEO (2026-07-02)** — dynamic **`/sitemap.xml`** (home + active products + published pages, lastmod) + **`/robots.txt`** (disallows admin/account/cart/checkout/orders; absolute sitemap URL) — both are ROUTES; the static `public/robots.txt` was deleted (a public file shadows the route). Product page: meta description + OG tags + **`Product`/`Offer` JSON-LD** (absolute `url` shipped from the controller); catalogue page: meta/OG. ⚠️ **hreflang deliberately skipped:** locale is session-based so AR/EN share one URL — hreflang requires distinct URLs per language (e.g. `/en` prefix); revisit only if that URL strategy is ever adopted.

### Infrastructure (TODO)
- [x] **Runtime SSR — code side (2026-07-02); production sidecar pending deploy.** `config/inertia.php` published (`INERTIA_SSR_ENABLED` env toggle, **default OFF** — dev stays CSR; + `timeout`/`connect_timeout` knobs) + **`App\Ssr\TimeoutHttpGateway`** ported from Sky Amman (hung sidecar → CSR fallback, not a 502; bound in `AppServiceProvider::boot` so it beats Inertia's register()-time binding) + `ssr.jsx` calls `i18n.changeLanguage(page.props.locale)` before render (SSR honors the session locale — verified: AR page renders AR server-side). **Verified end-to-end locally** (Sky Amman-style, not just a bundle poke): `node bootstrap/ssr/ssr.js` + `INERTIA_SSR_ENABLED=true php artisan serve` → curl shows real `<h1>` HTML in the body. **Two SSR-safety gotchas fixed** (each crashed the sidecar): (1) starter kit's `use-appearance.tsx` ran `window.matchMedia` at MODULE scope — ssr.jsx eagerly imports all pages, so the server died at import; now lazy + `typeof window` guarded. (2) `LanguageProvider` called `usePage()` while sitting ABOVE `<App>` → "usePage must be used within the Inertia component"; now takes an `initialLocale` prop passed from both entries. _At deploy: create the sidecar service (`node bootstrap/ssr/ssr.js`, private port 13714) + set `INERTIA_SSR_ENABLED=true`, `INERTIA_SSR_URL` (see Sky Amman's Railpack notes: `RAILPACK_DEPLOY_APT_PACKAGES=nodejs`, URL host baked at config:cache)._ **Update 2026-07-30: an "SSR service" already EXISTS in the Railway project** (currently offline with the rest — see the trial-expiry blocker), so don't create a second one; just verify its start command + the web service's two env vars once the project is back. The SSR bundle now ships with every `npm run build`, so there's nothing extra to build.
- [x] **CI (2026-07-02)** — GitHub Actions (`.github/workflows/ci.yml`): parallel jobs — PHP 8.2 + PHPUnit (in-memory SQLite) and Node 22 + **`tsc --noEmit`** (the real type gate; Vite/esbuild strips types without checking) + client + SSR builds. Enabler: **`withoutVite()` in `tests/TestCase.php`** — page-render tests no longer need a `public/build` manifest (gitignored → absent in CI; also retires the local "build before testing a new page" gotcha). **Playwright E2E job added 2026-07-26** (see "E2E suite" below) — a third CI job, **report-only** (`continue-on-error`) until it's proven stable. _Still manual: branch protection on `main`._ ⚠️ **Gotcha (found 2026-07-04, FIXED same day): first CI runs were all red on the frontend job.** _Symptom:_ `tsc --noEmit` dies with `TS2307: Cannot find module 'ziggy-js'` (`resources/js/app.tsx:8`) in CI while passing locally. _Root cause:_ `tsconfig.json` mapped `ziggy-js` → `./vendor/tightenco/ziggy` (Ziggy ships its JS inside the composer package), but the Node-only CI job never runs `composer install`, so `vendor/` is absent and the alias resolves to nothing. The Vite client/SSR builds passed even in CI because the `ziggy-js` import is type-only and esbuild strips it. _Fix:_ `ziggy-js@2.6.3` added as an npm devDependency (**exact-pinned to the composer version** — bump it whenever composer bumps `tightenco/ziggy`) + the tsconfig `paths` entry got a `./node_modules/ziggy-js` fallback after the vendor path. Verified by simulating the vendor-less CI locally (vendor path pointed at a nonexistent dir → `tsc` exit 0).
- [x] **E2E suite — Playwright (2026-07-26)** — browser tests of the critical customer paths in `e2e/` (storefront browse, cart add/qty/remove, guest bank-transfer checkout → confirmation, register + login, AR⇄EN locale toggle, plus the dark-mode regression guard and a visual-capture sweep). **Self-booting local target:** with no `QA_BASE_URL`, `playwright.config.ts`'s `webServer` runs `migrate:fresh --seed` + `artisan serve` against a **throwaway SQLite** (`database/e2e.sqlite`, gitignored) on **port 8100**, passing `DB_CONNECTION=sqlite`/`DB_DATABASE` via `env:` so the **MariaDB dev DB is never touched** (Laravel's real env wins over `.env`; validated). Deterministic fixture data = `CatalogSeeder` (`sukkari-1kg` etc.); the seeded admin is `admin@retab.com.sa` / `password`. Set `QA_BASE_URL=https://…` to run the same specs against a deployment instead (skips the local server). **Selectors:** a few `data-testid` hooks (`add-to-cart`, `cart-*`, checkout field names, `place-order`, `lang-toggle`) keep specs locale-independent; auth uses the native `name=` inputs. A shared fixture (`e2e/fixtures.ts`) pre-seeds the `retab_consent` cookie so the consent banner never intercepts clicks. Run: `npm run test:e2e` (needs a prior `npm run build` — the server serves built assets, no Vite dev server). **Card/Tamara checkout stops at the gateway boundary** (no live keys), so only bank-transfer is E2E'd end-to-end. Also covers the **admin notification bell** (`admin-notifications.spec.ts`: a real order reaching the bell + the live poll firing). 23 tests green locally; wired into CI as a **report-only** job (see CI note). ⚠️ **Report-only masked a real P1** on 2026-07-30 (the product-page white screen — the suite failed, CI still went green): **promote this job to a hard gate.** _Also next: coming-soon "I want this" + a returns flow._
- [x] **Production baseline data (2026-07-23)** — Railpack auto-runs `migrate --force` on deploy but **never `db:seed`**, so a fresh prod DB would boot with no settings and no legal pages. Fix: data migration `2026_07_23_100000_seed_production_baseline_data` calls the idempotent `SettingsSeeder` + `ContentPageSeeder` in `up()` (both `Setting::set`/`firstOrCreate` → safe to re-run every deploy), **skipped under `testing`** so the suite keeps its empty baseline. `ContentPageSeeder` was also wired into `DatabaseSeeder` (it was missing). **Admin user is deliberately NOT in the migration** (secret + run-once trap): `AdminUserSeeder` now reads `config('retab.admin.email'|'password')` (env `ADMIN_EMAIL`/`ADMIN_PASSWORD` via new `config/retab.php` — routed through **config, not `env()`**, so it survives `config:cache`), **refuses to create a weak default admin when `APP_ENV=production`**, and only auto-seeds the convenience editor in dev. Deploy step: set `ADMIN_*` env **before** the first deploy's optimize, then run once from the Railway console `php artisan db:seed --class=AdminUserSeeder --force`. Verified on a throwaway sqlite: `migrate --force` → 13 settings / 4 content pages / no admin. Tests in `tests/Feature/ProductionBaselineTest.php`.
- [ ] **Queue worker service on Railway** (`php artisan queue:work --tries=3 --timeout=60`) — a **second service off the same repo/branch**, no domain and **no healthcheck path** (a worker listens on no port, so a healthcheck crash-loops it), same env as web (`APP_KEY` identical; **`APP_URL` must be the real domain** or the admin emails' action buttons link to localhost). ⚠️ Keep `--timeout` **below** `retry_after` (90s in `config/queue.php`) or a long job gets handed to a second worker mid-run ⇒ **duplicate emails** (WhatsApp is safe — `SendWhatsappMessage` no-ops on an already-`sent` row). ⚠️ Only add `--max-time` if you also set **Restart Policy → Always**; the default On-Failure policy won't restart a clean exit, so the worker would silently stop. _Without it:_ admin email + WhatsApp never send, and the delayed review-reminder never fires (the bell still works by design — see Architecture → Notifications & alerts). _Interim option:_ `QUEUE_CONNECTION=sync` sends everything inline (pre-2026-07-30 behaviour, safe thanks to the job's sync-driver guard) except the 1-day-delayed review nudge. **Blocked until the client subscribes** (see below). Later, alerts like Tamara auth-expiry need a `schedule:work` service too.
- [ ] ⚠️ **BLOCKER — Railway trial expired (observed 2026-07-30): the whole prod project is OFFLINE.** MySQL, `retab-website` and the SSR service all show "Service is offline / no active deployment" until the **client picks a paid plan** — nothing can be deployed or added (including the worker above) meanwhile. Cheapest launch blocker to clear (a card, not a KYC review), so chase it alongside the payment/shipping accounts. ✅ Silver lining: prod never served the 2026-07-29 blank-product-page bug to a real customer. 🔴 **First action once MySQL is back: take a dump** — the real catalogue/settings/admin live only in that stopped `mysql-volume`, and an unpaid project is not a safe sole copy (use PDO or TablePlus; the XAMPP MariaDB CLI can't auth to MySQL 8's `caching_sha2_password`). That dump also doubles as the restore point before the demo-data rollback.
- [ ] Production deploy (MySQL, env vars, Railway project from client repo), Cloudflare DNS + Turnstile keys, mail domain verification, **R2 + Zid image re-host + real catalogue import** (Phase 2)
  - ⚠️ **Railpack build gotcha (hit 2026-07-23, FIXED): missing PHP extensions → build dies at `php artisan config:cache`.** _Symptom:_ first Railway build failed; Railpack's `install-php-extensions …` list installed `pdo_mysql redis` etc. but **not `zip`** (the FrankenPHP base image omits it), so composer's `vendor/composer/platform_check.php` fatals ("require ext-zip") on the first artisan call in the build. _Root cause:_ **`openspout/openspout`** (prod dep, Excel import/export) requires `ext-zip`/`ext-xmlreader`/`ext-xmlwriter`, and Railpack only installs the ext-* it detects, **not the transitive requirements**. Local (XAMPP) + CI (`setup-php` bundles them) both have these, so it only surfaced on Railway. _Fix:_ declare them in the **root `composer.json` `require`** — Railpack installs exactly the ext-* listed there. Added `ext-zip`, `ext-xmlreader`, `ext-xmlwriter`, and **`ext-gd`** (needed anyway for the `intervention/image` variant pipeline — `install-php-extensions gd` bundles WebP). Reproduce build cache locally with `php artisan config:cache && route:cache && view:cache && event:cache` then `optimize:clear`. _Still to verify at deploy:_ whether Railpack's start runs `migrate --force` automatically; if not, set a Railway **Pre-deploy Command** `php artisan migrate --force` (this also runs the baseline-data migration). Admin is one console step post-deploy: `php artisan db:seed --class=AdminUserSeeder --force`.

> **Last updated:** 2026-07-30 — **Guzzle security advisories actually cleared (7.12.3 → 7.15.2).** The 4 medium advisories outstanding since 2026-07-22 are fixed in 7.15.1/7.14.2, and `composer audit` now reports **"No security vulnerability advisories found."** ⚠️ Getting there exposed a trap worth knowing — a plain `composer update guzzlehttp/guzzle` installed a **dev-branch commit** (`7.12.x-dev`) and fixed nothing, because single-package updates don't touch that package's own dependencies and the starter kit allows dev stability; `--with-dependencies` was the fix. Full symptom→cause→fix under **Local Development → Composer gotcha**. Verified: **348 tests / 1570 assertions** still green after the upgrade (every payment/shipping/WhatsApp call goes through Guzzle via Laravel's Http client). _Noted for later: the wider tree is ~32 packages behind (Laravel 12.62→12.64, Inertia 3.1→3.2, Pint, aws-sdk…) — a deliberate maintenance pass of its own, not something to slip into a security fix._
>
> **Last updated:** 2026-07-30 — **Resend wired up, so the email channel is no longer inert.** Installed `resend/resend-php` (the framework's `ResendTransport` needs it); `config/mail.php`/`config/services.php` already had the mailer + `RESEND_KEY` binding, so no config edits. Client's key added to `.env` (gitignored — verified it appears in **no tracked file**). **Verified with a real send** to Resend's simulator address. Three findings worth keeping: (1) **the key is send-only/restricted**, so `GET /domains` returns 401 `restricted_api_key` — new **`App\Support\ResendProbe`** treats that as OK (invalid keys answer 400 `validation_error`, full-access keys 200 + domain statuses) and `integrations:check` now reports email beside the payment providers, including a warning when `MAIL_MAILER` isn't `resend` or the from-address is still the scaffold placeholder; (2) 🔴 **`retabstore.com` is NOT verified** — proven by an actual send failing with *"domain is not verified"*, so DKIM/SPF in Cloudflare DNS is the one remaining step and `onboarding@resend.dev` is the only usable from-address until then; (3) **dev stays on `MAIL_MAILER=log` on purpose** — the seeded staff have fake `@retab.com.sa` addresses and bouncing them off a fresh account would hurt its reputation, so new **`php artisan mail:test <address>`** sends through the resend mailer explicitly instead. `.env.example` documents the whole block. Verified: **348 tests / 1570 assertions** (+6), Pint ✓. _Note: `composer audit` still flags 4 pre-existing **guzzlehttp/guzzle** advisories (not from this change, outstanding since 2026-07-22) — worth a `composer update guzzlehttp/guzzle`._
>
> **Last updated:** 2026-07-30 — **Notification history page (`/admin/notifications`).** The bell keeps only the latest 10, so the 11th alert was unreachable. Added a paginated own-notifications list with **read-state + type filters** (whitelisted JSON-path where on `data->type`; verified on SQLite **and** MariaDB, and MySQL 8 is stricter-better), reached via a **"View all"** footer link in the dropdown; rows reuse the existing open route so clicking still marks read + redirects to the subject. Payload→text rendering was **extracted to `components/admin/notification-content.tsx`** (`useNotificationText`/`iconFor`/`NOTIFICATION_TYPES`) and both the bell and the page import it, so a new type can't render in one and not the other. ⚠️ **Gotcha: the page paginator is named `entries`, not `notifications`** — that name is the shared bell prop (`{unread, items}`) and a same-named page prop silently shadows it, which would make the bell throw on this very page (regression test pins it). No sidebar entry by design — notifications live on the navbar bell. Verified: **342 tests / 1553 assertions** (+5), `tsc` ✓, `lint` ✓, Pint ✓, and **3/3 admin-notification E2E specs green** including reaching the page through the bell and confirming the type filter empties the list. Full writeup in **Architecture → Notifications & alerts**.
>
> **Last updated:** 2026-07-30 — **Staff notifications: email channel, queued WhatsApp transport, live bell — plus a P1 product-page crash found while verifying.** Full writeup in the new **Architecture → Notifications & alerts**.
> - 🔴 **Fixed a white-screen on the product page (unrelated to this work, live since 2026-07-29 `c799da2`).** `ShopController::show` shipped the review-reward prop as **`review_reward`** while `shop/product.tsx` destructures **`reviewReward`** → `reviewReward.available` threw during hydration and **every visitor got a blank product page** (HTTP 200, HTML delivered, React dead). _Why nothing caught it:_ an Inertia prop-name mismatch is invisible to `tsc` (untyped boundary) and to PHPUnit status-code assertions, and the **E2E suite that did catch it is `continue-on-error` in CI**, so the run still reported success. _Fixed_ (camelCase, matching `hasOffers`/`turnstileSiteKey`/`lowStockList`) + a regression test pinning **every prop the page destructures** (`ShopControllerTest::test_product_page_ships_every_prop_the_page_destructures`). **Lesson: promote the E2E job to a hard gate** — it was already on the roadmap and would have blocked this.
> - **Email channel** on all three staff alerts (new order / return filed / product requested): `toMail()` + `ShouldQueue`, with mail **conditional on `$notifiable->email`** (nullable under the OTP identity model — the transport throws on an empty address) and **`viaConnections(['database' => 'sync'])`** so the bell row still lands in-request while only the email queues. Emails are deliberately **English literals** (the admin's panel language lives in their browser; a customer-triggered notification can't know it). The app previously sent **zero** business email despite the client brief asking for email alerts.
> - **WhatsApp transport moved onto the queue** — new `SendWhatsappMessage` job (tries 3, backoff 30/120/600s, idempotent). Every transactional send used to be an inline Meta HTTP call **per recipient inside the customer's request**; a slow API added seconds to checkout and a failure lost the alert to a log line. Ledger row still written synchronously as `queued`. Three deliberate subtleties: params are passed to the job (the ledger **redacts** OTP codes, so the row isn't a faithful payload), **OTP stays inline** (customer is waiting + keeps plaintext codes out of the `jobs` table), and the job **rethrows for retries except under the `sync` driver** so a Meta outage can never surface inside a checkout.
> - **The bell is live** — `router.poll(45s, { only: ['notifications'] })`, Inertia's own poller (hidden tab throttled to 1-in-10 ticks), so an order arriving while an admin sits on one page now surfaces without a manual reload. Plus a tab-title unread badge, an optional WebAudio chime (mute toggle persisted per browser) and a `notifications-dropdown` testid. ⚠️ **Gotcha: Inertia owns `<title>`** and rewrites it on hydration/navigation, silently dropping a badge written once (even one `requestAnimationFrame` late) — fixed with a `MutationObserver` on `document.head` + an equality guard against an observer loop.
> - 🔴 **New deploy prerequisite: a `queue:work` service** (see the Notifications section). Not new in spirit — `SendReviewReminder` and `SendWhatsappCampaign` already depended on a worker silently; now admin email + WhatsApp do too. The bell deliberately does not.
> - **Verified:** **337 tests / 1481 assertions** (+9), `tsc` ✓, `lint` ✓, 21 JS ✓, client+SSR builds ✓, and **23 E2E green** including a new `e2e/admin-notifications.spec.ts` that places a real order and asserts it reaches the bell, badges the tab title, and that a partial `notifications` poll fires in a real browser (server log confirmed the second request exactly 45s after the first).
>
> **Last updated:** 2026-07-29 — **Review-reward coupon (+ WhatsApp nudge) and an admin design-system pass.** One new feature and one consistency sweep.
> - **Review reward** (`c799da2`, `1fec321`, `50a142f`) — "write a review, get a discount": a verified buyer who reviews **with a comment** earns a one-time 10%/20% account-bound coupon valid 30 days, via `App\Services\ReviewRewardService` (row-locked, idempotent, once-ever per customer through the `loyalty_rewards` unique key). Off by default; managed on **`/admin/discounts`** (moved there out of Settings, and the page's columns were swapped so tools sit inline-start and current/history inline-end). A `SendReviewReminder` job dispatched with a **1-day delay** from `ShippingService` on delivery sends the WhatsApp nudge, deep-linked to a purchased product's `#reviews`, re-checking eligibility at send time and stamped by `orders.review_reminder_sent_at` (new migration) so a repeated "delivered" webhook can't re-send. Storefront shows the earn-nudge only while still earnable. Full writeup under **Coupons — DECIDED**; 195 lines of tests in `tests/Feature/ReviewRewardTest.php`.
> - **Admin design system** (`1b9441c`, `fd5c74b`, `3fb7bac`, `028b6e5`, `960c9ee`, `ced0f79`) — full writeup in the new **Architecture → Admin UI conventions**. Every ad-hoc status badge in the panel collapsed into one soft-tint `StatusPill` (dotted, dark-mode-safe tones); product visibility + coupon active became **one-click `StatusToggle`s** from the list (no confirm — reversible — with hover hints explaining blocked states); the undo/revert confirm got a styled dialog; the layout-shifting flash banner was replaced by **overlay toasts** (success auto-dismisses, errors linger); the inventory sync banner gained **health states + friendly relative time** (`iso` shipped alongside the exact stamp so the client renders "N days ago" via `Intl.RelativeTimeFormat` in the admin's own language). ⚠️ **The one real gotcha:** toasts needed a **per-response `flash.id` nonce** in `HandleInertiaRequests` — two consecutive actions flashing the *same* text wouldn't re-fire a value-compared effect dep, so the second toast never appeared.
> - **Storefront** (`fe44eee`, `d29a2bb`, `4ce92b3`) — homepage tile image for the rusks category, new stuffed-dates/rusk tiles, crisp gallery thumbnails (request the **`card`** variant, not the original) and clickable rating stars.
> - **Verified:** **328 tests / 1439 assertions** ✓ locally; CI green on `ced0f79` (PHPUnit + `tsc --noEmit` + client/SSR builds + the report-only E2E job).
>
> **Last updated:** 2026-07-27/28 — **Storefront + admin UX pass, and the SSR bundle now comes out of the default build.**
> - **Build** (`bdd1801`) — `npm run build` is now `vite build && vite build --ssr` (identical to `build:ssr`), so **Railway's default build emits the SSR sidecar bundle** with no extra build config. Costs a few seconds locally; removes a whole class of "sidecar has no bundle" deploy surprise.
> - **Storefront** (`f7e0107`, `e15c704`, `d8f1ae3`, `dd7312d`) — product image **hover-zoom + full-screen viewer** (new `store/product-gallery.tsx` + shared `image-lightbox.tsx`), **one-click SKU copy** (`copy-text.tsx`, storefront + admin), an **Offers home section** whose nav entry **hides when nothing is discounted** (`hasOffers` — a cheap `EXISTS` closure on the shared props, so it resolves only for full Inertia page loads), and the offers carousel watermark mirrored to the right.
> - **Admin** (`925c529`, `6502089`, `e1483eb`, `0065ce1`) — a **card⇄table view toggle** on Products (persisted in `localStorage`, display-only), click-to-zoom/swap on the product image, mobile scroll-to-top/bottom floating buttons, and a **per-page size selector** on every list table backed by the new whitelisted `Controller::perPage()` helper (see Admin UI conventions).
>
> **Last updated:** 2026-07-26 — **Full security audit + hardening pass.** Swept the whole attack surface (authZ/IDOR, admin permission gating, checkout price integrity, all four webhook signatures, input validation, file upload, rate limits/CSRF/headers/proxies, data exposure, injection, mass assignment). **No Critical/High issues** — the payment layer (re-fetch + row-lock + amount/currency check, tampering logged never fulfilled), webhook signature verification, OTP (hashed/expiry/single-use/5-attempt cap), IDOR gating, and admin user-management are all genuinely solid. Applied six hardening fixes with tests: (1)+(2) added per-IP `throttle` to `login`/`forgot-password`/`reset-password` (login also keeps the per-email+IP `LoginRequest` limiter); (3) **removed `role`+`permissions` from `User::$fillable`** (privilege fields) — set only via `forceCreate`/`forceFill` at the trusted setters (Admin\UserController, OtpAuthController, AdminUserSeeder wrapped in `User::unguarded`); ⚠️ **gotcha:** Eloquent **factories bypass `$fillable`** (verified — `User::factory()->create(['role'=>...])` still works), but non-factory `User::create([...role...])` respects it, so the ~9 test helpers using it were switched to `User::forceCreate`; (4) capped cart `quantity` at `max:99`; (5) `lockForUpdate()` on the coupon row in `CheckoutService::resolveCoupon` (kills the check-then-increment over-redemption race); (6) added the `admin` route middleware to `settings/reset` (belt-and-suspenders with its in-controller `isAdmin` check). New regression tests in `tests/Feature/Security/SecurityHardeningTest.php` (role can't be mass-assigned, profile update can't escalate, cart cap, login + forgot-password rate limits). Deferred (noted, low priority): nonce-based CSP to drop `script-src 'unsafe-inline'`; sanitize `Media` `$dir` against `..`. Verified: **310 tests / 1343 assertions** (5 new), Pint ✓.
>
> **Last updated:** 2026-07-26 — **`integrations:check` readiness command + `.env.example` integration docs (payment/shipping activation prep).** The Moyasar/Tamara/OTO service layer was already built and wired (checkout, admin-confirm capture/void, refunds, OTO fulfill on confirm); what's left is purely **activation** (the client's accounts + keys). To make that fast and verifiable: added a non-throwing **`ping()`** to `MoyasarGateway` / `TamaraClient` / `OtoClient` (each reuses its real auth: Moyasar lists invoices, Tamara reads a bogus order so 404=authed vs 401=rejected, OTO does the refresh-token exchange) + **`php artisan integrations:check`** (`{--offline}`) — per provider it reports env presence, the exact **webhook URL to register** (from APP_URL), and a live auth probe (OK / FAILED / NOT CONFIGURED); exit code fails only when a *configured* provider fails, so it's deploy-smoke-check-safe. The command's docblock doubles as the activation reference. **`.env.example`** gained a documented "External integrations" block (Turnstile, Moyasar, Tamara, OTO, WhatsApp, R2) with where-to-get + webhook notes, so every client-supplied key has a labeled home. Verified all three probe states (not-configured / bad-key→401 / offline) + **305 tests / 1333 assertions** (8 new, Http::fake), Pint ✓. _Reminder: these stay NOT CONFIGURED until the client opens Moyasar/Tamara/Tryoto accounts and shares keys; Tamara is likely reusable from the Zid setup, Moyasar/OTO probably new._
>
> **Last updated:** 2026-07-26 — **Playwright E2E suite (critical customer paths) + report-only CI job.** Added browser tests in `e2e/` covering storefront browse (home/catalogue/category/search/product), cart (add/qty/remove), guest bank-transfer checkout → confirmation, register + login + guest gating, and the AR⇄EN locale toggle — plus the existing dark-mode guard and a slug-agnostic visual-capture sweep. Full writeup in Infrastructure → "E2E suite": the config self-boots a **freshly-seeded throwaway SQLite** app (`webServer`, port 8100, `DB_*` via `env:` so the MariaDB dev DB is untouched — validated) when no `QA_BASE_URL` is set, else runs against that deployment. Added a handful of `data-testid` hooks + a shared consent-cookie fixture (`e2e/fixtures.ts`) so specs are locale-independent and the banner never blocks clicks. Card/Tamara checkout stops at the gateway boundary (no live keys) so only bank-transfer is fully E2E'd. **21 tests green locally**; CI gained a third **report-only** (`continue-on-error`) job that builds assets + installs chromium + runs the suite (promote to a hard gate once stable). `tsc` ✓, `lint` ✓ (renamed the Playwright fixture arg `use`→`run` to dodge a react-hooks false positive). Also added `deployment_phase` to the CI push triggers (the active branch wasn't running CI).
>
> **Last updated:** 2026-07-25 — **Zid 301 redirect map + Arabic slug cleanup (SEO cutover safety).** The Zid import seeded `products.slug` from the old Zid `product_page_url`, and Zid used the same `/products/{slug}` path we do (verified live), so the currently-indexed URLs resolve today — but any slug we clean would 404 and drop its Google ranking. Built (see Architecture → SEO for the full writeup): a **`redirects` table** + `App\Models\Redirect` (301 map, targets the product so it follows the current slug), a **`catalog:clean-slugs {--apply}`** command that regenerates only the **junk** slugs (ar-prefixed / English / uppercase / leading-hyphen) into clean **Arabic** from `name_ar` via **`App\Support\ArabicSlug`** (preserves Arabic + Arabic-Indic digits; Str::slug would transliterate them away), leaving already-clean Arabic slugs untouched (no redirect → fewest 301s → best ranking preservation), and a **`->missing()`** hook on `/products/{product:slug}` → `RedirectController` that 301s old→new (route:cache-safe via SerializableClosure — verified). Client chose Arabic slugs over English. On the real catalogue: **27 of 87** slugs cleaned; applied locally + curl-verified (old English/ar- slug → 301 → Arabic, new slug → 200, garbage → 404). **Launch step:** run `catalog:clean-slugs --apply` on prod once final slugs are settled. Verified: **297 tests / 1318 assertions** (18 new), Pint ✓, `route:cache` ✓.
>
> **Last updated:** 2026-07-23 — **Deploy Phase 1: production baseline data + prod-safe admin bootstrap (git hygiene too).** Railpack runs `migrate --force` on deploy but not `db:seed`, so a fresh prod DB would come up with no settings/legal pages (Moyasar activation needs a live policy page). Fix (see Infrastructure → "Production baseline data"): a data migration (`2026_07_23_100000_seed_production_baseline_data`) runs the idempotent `SettingsSeeder` + `ContentPageSeeder` on every deploy, skipped under `testing`; `ContentPageSeeder` wired into `DatabaseSeeder` (was missing). Admin stays out of the migration — `AdminUserSeeder` now pulls creds from `config/retab.php` (`ADMIN_EMAIL`/`ADMIN_PASSWORD`, **config not `env()`** so `config:cache` doesn't null them), **refuses a weak default admin in production**, editor seeded in dev only; run once post-deploy via `db:seed --class=AdminUserSeeder --force`. Verified on a throwaway sqlite (13 settings / 4 pages / no admin) + `ProductionBaselineTest` (5 tests). **Real 87-product catalogue is Phase 2** (needs R2 + Zid-image re-host — `CatalogSeeder` is placeholder demo data only, not the real import). Also: **git workflow locked down** — deleted the divergent `construction_phase`/`deployment_phase` history churn, unified both repos' `main` to `1a23356`, added a `git ship <branch>` alias (local merge + dual-push + re-sync the branch to main so it never shows "behind"; guards against shipping `main`) and the rule **never use GitHub's merge button while origin has two push URLs** (that's what diverged the mains). Verified: **274 tests / 1257 assertions** (5 new), Pint ✓.
>
> **Last updated:** 2026-07-22 — **Responsive WebP image variants + instant in-memory catalogue search + first-letter typeahead + grouped admin sidebar.** Image pipeline (see Architecture → File Storage → "Responsive WebP variants"): `intervention/image` v3 + `config/media.php` + `App\Support\ImageVariants` auto-generate thumb/card/detail WebP through the `Media` layer (`storeImage` generates, `url($path,$variant)` serves, `delete` cleans up); catalogue→`card`, search→`thumb`, product→`detail`; `php artisan media:variants` backfilled all 163 images (92 KB JPEG → 15 KB card / 3 KB thumb). Reusable pattern saved to memory (`image-variant-pipeline`). **Search** rebuilt as Tier-1 in-memory: `GET /shop/search-index` serves the whole small **cached** catalogue (busted on any product/image save via model events), fetched **once** on first focus then filtered in the browser — zero DB/round-trip per keystroke, SSR/SEO untouched (lazy fetch, not a prop; crawlers never trigger it); triggers from the **first character**; slim brand-gold scrollbar on the dropdown. **Admin sidebar** grouped into labeled sections (Sales / Catalog / Promotions / Content / System) + the Requests page got its "How it works" drawer. Decision recorded: **no Redis needed** — `database` cache/session/queue (Laravel 12 default) is right for this scale on Railway; add Redis only for multi-instance scale or high-volume queues (env flip, no code change). Verified: **268 tests / 1233 assertions**, `tsc`/`lint`/client+SSR builds ✓. _(Pre-existing `guzzlehttp/guzzle` advisory surfaced by composer audit — unrelated, worth a separate `composer update`.)_
>
> **Last updated:** 2026-07-22 — **Coming Soon + "I want this" demand capture + admin drafts workspace + catalogue data cleanup.** Turns the 22 hidden Zid-import drafts from dead weight into a demand funnel (see Architecture → Coming Soon + Demand Capture). New `products.is_coming_soon` display flag (buyability still `is_active`-only — cart unchanged, so coming-soon 404s the cart) + `product_requests` table + `product_requests` permission section. Storefront: `scopeVisibleOnStore` powers catalogue/detail, coming-soon cards show a badge + "I want this", the product page renders a request control (`POST /products/{slug}/request`, guests+phone+Turnstile, one open request per customer, staff-notified via `ProductRequestedNotification`). Admin: Products "Drafts" status filter + completeness badges, product-form Coming-Soon toggle, dashboard `draftsToComplete` task, dedicated `/admin/product-requests` page (list + mark-handled). Data: all 22 inactive drafts given draft bilingual descriptions (only-empty fills; price/photo stay client-supplied). Earlier same day (separate commits): **English names added to all 87 products** (variety transliterations verified — Ushaiger/Sukkari/Khalas/Sagai/etc., `name_en` importer guard added so re-imports don't wipe them), **Zid SKU replaced by our `RTB-####` code** (smacc_sku kept), **two "شابورة بالقمح" duplicates disambiguated** (Retab Wheat Rusk bag vs Al Rashaqa Original Rusk box), **catalogue filter now hides empty categories** (the duplicate parent "Dates" nav-group + any product-less category no longer appear as chips), and a **card-thumbnail follow-up** noted under Architecture → File Storage (deferred to the designer's final images). Verified: **260 tests / 1197 assertions** (11 new), `tsc` ✓, `lint` ✓, 21 JS tests ✓, client + SSR builds ✓.
>
> **Last updated:** 2026-07-18 — **Brand font switched to self-hosted Thmanyah Sans (from bunny.net Tajawal) + admin notification bell.** Typography: replaced Tajawal with **Thmanyah Sans** (ثمانية typeface, licensed) across the RTL `--font-sans` + `--font-heading` tokens in `app.css`; WOFF2 (Regular/Medium/Bold/Black) self-hosted in `resources/fonts/`, `@font-face` with `font-display: swap`, Vite-hashed, Regular+Bold preloaded in `app.blade.php` (`crossorigin`); dropped Tajawal from the bunny.net request (Instrument Sans stays the Latin/admin default). Client chose the conservative all-Sans swap over serif-display headings. See Architecture → Bilingual for the full **licence caveats** (no subsetting, WOFF2-only, get written OK from thmanyah before launch). Earlier the same day: **admin notification bell** — `database`-channel `NewOrderNotification` + `ReturnRequestedNotification` dispatched to all staff, shared via `HandleInertiaRequests`, rendered in a navbar `NotificationBell` (client-localized, `Intl.RelativeTimeFormat` via `lib/relative-time.ts`), `NotificationController` (open/read-all) scoped per user. Verified: 211 tests / 929 assertions, client build ✓, fonts emitted + hashed + manifest-resolvable.
>
> **Last updated:** 2026-07-06 — **Change log v2 built (audit + revert across the admin).** Extended `activity_logs` with `old_data`/`new_data`/`label`/`reverts_log_id`; new `App\Services\ChangeLog\ChangeLogService` + `RevertResult`; logging wired into products / settings / content pages (each write+log pair transactional); staff page at `/admin/change-log` (list, field diffs, revert, revert-of badges); SMACC imports appear audit-only (their undo stays on the Inventory page). Semantics deliberately fix Sky Amman's failure catalog: dirty-fields-only update snapshots (no full-record clobber), per-field conflict detection (refuses when a field changed again — also prevents out-of-order resurrection), transactional reverts with honest reason codes, and reverts logged as first-class mirror entries via `reverts_log_id` (redo = revert the mirror). Deliberately excluded: orders/returns/payments (own `order_activities` audit + state machines). Verified: **153 tests / 565 assertions** (12 new), `tsc --noEmit` clean, client build ✓, MariaDB migration ✓.
>
> **Last updated:** 2026-07-04 — **CI fixed + `construction_phase` merged to `main` in BOTH repos + merge workflow decided.** The ziggy-js CI fix (`3055f1e`, see Infrastructure → CI gotcha) went green and both PRs were merged (own repo PR #4 → `bc5a64c`; client repo → `f9934fa`). Those two separate Merge clicks surfaced a new dual-push fact: **the two `main`s are now permanently SHA-divergent** — investigated, found Sky Amman works the same way, and **decided to keep the both-buttons merge workflow** (push branch → PR in each repo → check CI → merge both; never push `main` from local — full rationale + gotcha logged in Git & Deploy → Merge workflow). Deploy-safety side effect: local `main` pushes can't reach the client repo, so no accidental Railway builds once it's wired.

> **Last updated:** 2026-07-04 — **Status audit + doc corrections (no code changes).** Verified where the project stands: tree clean, `construction_phase` pushed everywhere, **141 tests / 501 assertions green locally** + local `tsc --noEmit` clean. Three doc corrections: (1) **client repo is populated + write access granted** — dual-push fully working, both branches in sync with origin (the "pending collaborator invite / repo empty" notes were stale); (2) **GitHub CI was RED** — `tsc` couldn't resolve `ziggy-js` in the Node-only job because the tsconfig alias points into `vendor/` (gotcha under Infrastructure → CI; **fixed later same day** — `ziggy-js@2.6.3` devDep + node_modules paths fallback — unblocking PR #4 `construction_phase`→`main`); (3) refreshed the doc-sync stamp (the two 2026-07-02 doc commits hadn't). Next up: fix CI, then merge `construction_phase` → `main` (PR #3 already merged up through the SSR/SEO commit; main is 4 commits behind — marketing, CI + Tajawal, 2 doc commits; pushing `main` will eventually drive the Railway prod deploy), then the Production-deploy checklist.

> **Last updated:** 2026-07-02 — **Returns module built end-to-end (+ real gateway refunds).** Customer flow: `GET/POST /orders/{order}/return` (owner-gated; eligibility = delivered + 3-day window + no open return, enforced in `ReturnService`), photos through `Media` to `returns/{order_id}`, bilingual `shop/return-request` page + status/entry-button on the order page. Admin flow: `/admin/returns` list/detail with photos + a refund preview for both shipping toggles → approve/reject → exchange or refund. **New refund plumbing:** `refundPayment()` added to the `PaymentGateway` contract (Moyasar impl) + `PaymentService::refund` and `TamaraClient/TamaraService::refund`; refund ledger rows record the ACTUAL amount (a direct `Payment::create` — `recordTransaction` assumes order-total, wrong for partials); `payment_status` moves to `refunded`/`partially_refunded` off the summed refund ledger. `OrderConfirmationService::releaseFunds` card-refund TODO retired (real refund, best-effort). WhatsApp `return_update` (customer) + `admin_return_requested` (admins) templates added — remember to create + get them approved in Meta before launch. Verified: **123 tests / 428 assertions** (5 new: filing+photos, window, duplicate-block, ownership, admin refund partial-math) + client & SSR builds.

> **Last updated:** 2026-07-02 — **Killed the recurring Intelephense P1005/P1013 false positives — for the whole machine, not just this repo.** True root cause was **not** Eloquent magic itself: the official Laravel VS Code extension generates `vendor/_laravel_ide/_model_helpers.php` with defaults-less `@method static mixed where($column, $operator, $value, $boolean)` tags that shadow the real signatures, **and** a second embedded Intelephense (the `porifa.laravel-intelephense` fork, no diagnostics settings) kept re-reporting it — which is why the old global `argumentCount: false` never worked. Fixed by uninstalling the fork + excluding `**/vendor/_laravel_ide/**` via global `intelephense.files.exclude` (restored `argumentCount` checking). Kept `barryvdh/laravel-ide-helper` in-repo for correct signatures + model autocomplete (self-heals via `post-update-cmd`). Full gotcha in Local Development. 118 tests still green.

> **Last updated:** 2026-07-02 — **Arabic-first i18n + RTL foundation, then storefront localization (AR⇄EN).**
> - **i18n foundation** (`b94cd6d`): `SetLocale` middleware (default **AR**) + `LocaleController` + `POST /locale/{locale}`; `HandleInertiaRequests` shares `locale`; `<html dir>` set from locale in `app.blade.php` (RTL on first paint, no flash); installed `react-i18next` + `i18next` (runtime deps → survive SSR prune) with `resources/js/i18n/{ar,en}.ts` bundles; `LanguageContext` (session-cookie source of truth, no localStorage) wired into `app.tsx` + `ssr.jsx`; AR⇄EN toggle in the store header.
> - **Storefront localization** (`da3f36f`): all 10 storefront/account/auth pages + `store-layout` converted to `t()` (incl. the order-status map, GCC country names, payment-method labels that were inline Arabic dicts). DB content (product/category names) localized via **`useLocalized()`** — controllers (`ShopController::card`, `CartService::summary`, `WishlistController`) now ship both `_ar`/`_en` and the client picks by locale so the toggle is **instant** (no reload), mirroring Sky Amman. Verified: client build ✓, SSR build ✓, 118 tests / 408 assertions ✓, zero Arabic literals left in converted pages.
> - **Server-side localization** (same-day follow-up): flash/error strings → `__('messages.*')` (`lang/{ar,en}/messages.php`) across storefront + admin controllers/services; Laravel validation/auth/passwords localized (`lang/ar/*` mirroring published `lang/en/*`, with AR `attributes`). 118 tests still green (AR values equal the old literals, so flash assertions hold). ⚠️ Resolves at request time — follows on the next request, not the instant client toggle.
> - **Deferred (distinct mechanisms):** admin-panel UI i18n (still EN/LTR by design), Arabic web font (Tajawal / IBM Plex Sans Arabic), and honoring the session locale in SSR once runtime SSR lands.
> - **Also:** corrected the stale Build Progress checklist — the storefront vertical + much of admin were already built but still showed as TODO.

> **Last updated:** 2026-07-02 — **Adopted two cross-project doc-maintenance conventions from Sky Amman.**
> - **Doc-sync stamp** added at the top of this file: anchors CLAUDE.md to a git commit (hash + date + time via `git log -1`), refreshed on every doc edit — a machine-anchored freshness marker that pairs with this prose log. Seeded to `a50e0c8` (2026-07-01).
> - **"Log the tricky stuff" convention** added: document any issue/blocker/non-obvious behavior with symptom → root cause → fix (inline in retab's style and/or a line here) so it isn't re-hit — including in sibling projects on the same stack.

> **Last updated:** 2026-06-28 — **Scaffold + CLAUDE.md, Inertia v2 → v3, local DB → XAMPP MariaDB, dual-push to client repo wired.**
> - **Dual-push to client repo configured:** added `https://github.com/retab-dates-dev/retab-website.git` as a 2nd push URL on `origin` (now 1 fetch + 2 push URLs) — this is the **Railway production source**. Read access verified (`ls-remote` exit 0); repo is empty so first push will populate it. Chose dual-push (one `git push` → both repos) over a separate `client` remote.
> - **Local DB configured + migrated:** `.env` points at XAMPP **MariaDB 10.4.32** (`127.0.0.1:3307`, db `retab-stores`, root / empty password); ran `php artisan migrate` (users/cache/jobs + sessions tables created). Confirmed the engine is **MariaDB**, not MySQL — XAMPP ships MariaDB under the "mysql" name (binary `C:\xampp\mysql\bin\mysqld.exe`, service `mysql`); TablePlus was just the client used to create the DB. `APP_KEY` already present from scaffold. Flagged dev↔prod DB parity (prod planned MySQL 8) as a pending decision.
> - **Inertia v2 → v3 upgrade** (on branch `construction_phase`, before any feature work): `inertia-laravel` v2.0.24→v3.1.0, `@inertiajs/react` ^2.0→^3.0 (3.5.0). Verified the doc's "v2 vs v3" framing against both lockfiles (Sky Amman really is v3.0.4) and confirmed v3 needs only PHP 8.2 — the PHP-8.3 pin was about the scaffolding template, not Inertia. Only the scaffold's `resolvePageComponent` resolve needed changing (v3 dropped default-export auto-unwrap → added `.then(m => m.default)` in `app.tsx`/`ssr.jsx`, mirroring Sky Amman). Build + 26 tests green. This resolves the former "Inertia is v2 here" divergence from Sky Amman.
> - Scaffolded `retab-stores` with the official Laravel 12 React starter kit (Inertia v2 + React 19 + TS + Tailwind v4, PHP 8.2 / starter kit v1.0.1). Added `.npmrc` before install to dodge the `NODE_ENV=production` devDep trap; production build verified clean. Git on `main`, `origin` → `mercenary19961/retab-stores` (dual-push to the client repo to be added once it exists).
> - Wrote this `CLAUDE.md` adopting Sky Amman's conventions (Build Progress checklist, `> Last updated:` log, commit convention + commit-suggestion rule, code-quality rules) and set **Sky Amman as the reference project**, with an explicit "don't blindly copy" list (Inertia **v2** not v3, lowercase `pages/` + modular routes, shadcn/Radix base, no i18n/Turnstile/admin yet).
> - **SSR noted as the immediate next task:** build-time scaffolding (`ssr.jsx` + `build:ssr`) is present, but runtime SSR (config/inertia.php toggle + graceful-fallback gateway + prod sidecar) still needs wiring — to be ported from Sky Amman.
